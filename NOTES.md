## [2026-09-18] Tổ trưởng khai kế hoạch máy — Đính chính: URL test suốt bước 2-7b thật ra là `/exec` thật
- **Phát hiện quan trọng:** URL được gọi là "Triển khai thử nghiệm" dùng để test từ bước 2 tới giờ (`.../exec`) thật ra **chính là URL `/exec` production** — không phải một bản test riêng tự động cập nhật theo mã HEAD như đã hiểu nhầm. Lý do mọi thay đổi "có tác dụng" khi test là vì **chủ dự án đã tự deploy lại (đúng quy trình an toàn) sau mỗi lần `clasp push`**: Apps Script Editor → Triển khai → Quản lý bản triển khai → chọn bản đang có → bút chì → Phiên bản mới → Triển khai — giữ nguyên deployment ID.
- **Không gây hại:** deployment ID giữ nguyên xuyên suốt (đúng quy trình `bao-tri-v2/CLAUDE.md` đã ghi), URL `/exec` không đổi, 175 QR đã in không bị ảnh hưởng. Mã mới chỉ thêm route/file riêng (`KeHoachTo.gs`, `ToTruong.html`, nhánh `page=kehoach`), không đụng luồng công nhân/thợ đang chạy thật.
- **Hệ quả cho các mục ghi "CHƯA deploy" ở các bước 1-7b bên dưới:** những dòng đó **không chính xác** — thực tế đã deploy lại nhiều lần trong quá trình test tay (chủ dự án tự làm, không qua xác nhận riêng từng lần với tôi vì tôi không biết đó là bước deploy). Không sửa lại các mục cũ (giữ nguyên lịch sử), chỉ đính chính tại đây.
- **Thay đổi quy trình từ giờ:** mỗi lần cần test trên link thật, tôi sẽ xin xác nhận **CẢ HAI bước** `clasp push` **và** deploy (đúng quy trình an toàn ở trên) thay vì chỉ nói push như trước — vì đây là thay đổi trực tiếp lên hệ đang chạy thật, không phải một bản test tách biệt.

## [2026-09-18] Vá `themMayMoi()` — ĐÃ `clasp push` + deploy
- **Thay đổi:** Không sửa mã thêm. Chủ dự án xác nhận đã `clasp push` và deploy lại (đúng quy trình an toàn, giữ nguyên deployment ID).
- **Trạng thái:** Đã push GitHub (commit `482ae0e`), **ĐÃ `clasp push`**, **ĐÃ DEPLOY**. Bản vá `themMayMoi()` dùng `soDongCoDuLieu_` đang chạy thật — lần tới thêm máy mới vào `MAY_THEM_MOI` và chạy menu sẽ ghi đúng vị trí, không còn lạc xuống dòng ~1000.
- **Việc cần làm tiếp theo:** Không có gì khẩn cấp — theo dõi bình thường, chỉ cần nhớ bản vá đã sẵn sàng cho lần thêm máy tiếp theo.

## [2026-09-18] Vá lỗi `getLastRow()` xác nhận thật ở `themMayMoi()` (`DanhMuc.gs`)
- **Xác nhận bằng kiểm tra tay trên Sheet thật:** `Ctrl+End` trên `Danh_Muc_May` nhảy tới dòng **1000** dù chỉ có ~162 máy thật — đúng lỗi `getLastRow()` bị `datCheckbox_` (cột `Hoat_Dong`) làm sai, cùng cơ chế đã vá ở tính năng tổ trưởng (`Lich_Lam_Viec_To`/`Ke_Hoach_May`).
- **Tin tốt — không có dữ liệu bị mất/lạc:** `CMTD02`/`CMTD03` (thêm trước đây qua `themMayMoi()`) nằm đúng dòng 161-162, ngay sau `MCQTX02` (dòng 160) và trước `BT01` (dòng 163) — đúng thứ tự liền mạch. Nghĩa là lúc `themMayMoi()` chạy, `getLastRow()` vẫn còn tính đúng; checkbox chỉ "lan" tới dòng 1000 ở một lần `setupSystem()` chạy SAU đó. Rủi ro chỉ là **tương lai**: lần tới thêm máy mới sẽ bị ghi lạc xuống dòng ~1001.
- **Đã vá:** `themMayMoi()` đổi từ `sh.getRange(sh.getLastRow() + 1, ...)` sang `sh.getRange(soDongCoDuLieu_(sh, 1) + 2, ...)` — dùng lại hàm `soDongCoDuLieu_` đã có sẵn trong `Code.gs`. Đúng 1 dòng thay đổi, không đụng gì khác.
- **Chưa vá `dongBoDanhMucBoPhan_`** (cùng file) dù hàm này cũng đọc `sh.getLastRow() - 1` — đã xem lại, hàm này **không sai kết quả** (tự lọc dòng `Ma_May` trống, phần dọn dư ở cuối vô hại), chỉ đọc/dọn dư vài trăm dòng trống không cần thiết. Không sửa vì không phải bug, tránh đụng thêm vào file production ngoài phạm vi đã duyệt.
- **`kiem-tra.ps1` sạch cả 5 lớp.**
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push` + deploy (2 bước) nếu muốn bản vá có hiệu lực thật — không khẩn cấp vì chưa có nhu cầu thêm máy mới ngay, có thể vá xong để đó, làm khi thật sự cần chạy `themMayMoi()` lần nữa.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 8/8 (bước cuối): test + tài liệu
- **Thay đổi:** Đối chiếu đủ 22 mục kiểm thử bắt buộc ở `plan18.9.md` mục 17 với bộ test đã có — 18 mục đã có test từ bước 3-5 (đánh số ngay trong tên test để đối chiếu), thêm 4 test mới vào `Test.gs`: **#3** tổ DET không lấy được máy SOI, **#9** đóng máy cả tuần (7 ngày × 2 ca = 14 lượt), **#18** ca đêm ở ngày cuối tuần không làm lệch `Ngay` đã chọn, **#19** Chủ nhật khai đóng máy bình thường. Mục #17 (hàng loạt) và #15 (reload) đã xác nhận bằng test tay ở các bước trước; #20 (không đổi `Hoat_Dong`) và #21 (không tạo dòng `Su_Co`) xác nhận bằng đọc mã — không hàm nào trong `KeHoachTo.gs` ghi vào `SHEET.MAY` hay tham chiếu `SHEET.SU_CO`; #22 (luồng QR vẫn xanh) xác nhận vì toàn bộ test thợ/công nhân cũ chạy chung trong đúng lượt `chayTest()`.
- Cập nhật `bao-tri-v2/CLAUDE.md`: thêm **mục 2b** mô tả đầy đủ tính năng tổ trưởng (file, schema, route, RPC, bẫy `getLastRow()` đã vá, trỏ sang `TASK_KE_HOACH_TO_TRUONG.md`). Cập nhật bảng file (22→24 file), schema (11→11+3 sheet), và sửa số test "340" đã lệch từ trước (do lần kéo trực tiếp từ Apps Script Editor 18/09 không cập nhật tài liệu) thành đúng **401**.
- **`kiem-tra.ps1` sạch cả 5 lớp.**
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`.** Về deploy: xem mục đính chính ở trên — thực tế đã deploy nhiều lần trong quá trình test tay bước 1-7b (giữ nguyên deployment ID, không ảnh hưởng QR).
- **Việc cần làm tiếp theo:** Xác nhận `clasp push` + deploy (2 bước, đúng quy trình an toàn) để đưa bản có đủ test + tài liệu lên, chạy menu 🧪 xác nhận đúng 401/401. Sau đó **TOÀN BỘ 8 BƯỚC CỦA ĐỢT 1 (TỔ TRƯỞNG KHAI KẾ HOẠCH) HOÀN TẤT** — chỉ còn việc `git push` lên GitHub (repo hiện đang vượt trước `origin/master` nhiều commit, toàn bộ chỉ nằm ở máy) khi chủ dự án sẵn sàng.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7b/8: ĐÃ XÁC NHẬN, bước 7b hoàn tất
- **Thay đổi:** Không sửa mã. Chủ dự án xác nhận đủ 6 luồng: đóng hàng loạt máy đã chọn, đóng/bố trí tất cả 48 máy (số liệu 48/0 khớp đúng), sao chép kế hoạch 1 ngày sang nhiều ngày đúng, tìm kiếm + lọc "chỉ xem máy đang đóng" đúng, Lưu kế hoạch tuần + F5 giữ nguyên dữ liệu.
- **Trạng thái:** Bước 7b hoàn tất, xác nhận chạy thật đúng thiết kế. Toàn bộ giao diện tổ trưởng (bước 6-7b) đã xong: khai giờ làm việc lần đầu, xem/đổi tuần, đóng-mở từng máy, thao tác Chủ nhật, thao tác cả tuần, chọn nhiều máy, sao chép kế hoạch, tìm kiếm, lọc.
- **Việc cần làm tiếp theo:** Bước 8 (bước cuối) — thêm 22 test case theo mục 17 `plan18.9.md` vào `Test.gs`, cập nhật `bao-tri-v2/CLAUDE.md` (thêm mục tính năng mới + sửa lại số test đã lệch từ trước) và `NOTES.md`.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7b/8: thao tác hàng loạt
- **Thay đổi:** Thêm vào `ToTruong.html`: checkbox chọn nhiều máy + "Chọn tất cả"/"Bỏ chọn tất cả" (theo đúng danh sách đang lọc, không phải toàn bộ tổ), thanh hành động khi có máy được chọn ("Bố trí chạy" / "Đóng máy…"). Nút "Bố trí tất cả máy chạy"/"Đóng tất cả máy" cho toàn tuần — khác 2 nút Chủ nhật đã có ở bước 7a. "Sao chép kế hoạch": chọn 1 ngày nguồn + nhiều ngày đích, copy nguyên trạng thái từng máy sang, ghi đè đúng ngày đích. Ô tìm máy theo mã/tên (không phân biệt dấu, dùng `normalize('NFD')`) + lọc "chỉ xem máy đang đóng" — chỉ ảnh hưởng HIỂN THỊ, không đụng `NGOAI_LE`.
- Tổng quát hoá modal đóng máy: `MAY_DANG_MO` (1 máy) → `DS_MAY_DANG_MO` (mảng), dùng chung logic cộng-dồn-theo-khoá `ngày|ca` cho cả trường hợp 1 máy lẫn nhiều máy — không viết trùng code.
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 662 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, thử: chọn nhiều máy → đóng hàng loạt; "Đóng tất cả máy"; sao chép kế hoạch 1 ngày sang nhiều ngày; tìm kiếm + lọc; Lưu + F5. Sau đó sang bước 8 (22 test + cập nhật `bao-tri-v2/CLAUDE.md`/`NOTES.md`).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7a/8: ĐÃ XÁC NHẬN toàn bộ, bước 7a hoàn tất
- **Thay đổi:** Không sửa mã. Chủ dự án xác nhận cả 4 phần đã sửa trong bước 7a: chọn nhiều ngày trong modal đúng, cộng dồn qua nhiều lượt "Sửa" đúng (không mất lượt trước), 2 nút thao tác nhanh Chủ nhật hoạt động hoàn hảo, Lưu kế hoạch tuần + F5 giữ đúng dữ liệu.
- **Trạng thái:** Bước 7a hoàn tất toàn bộ, xác nhận chạy thật đúng thiết kế qua nhiều vòng chỉnh sửa theo phản hồi thực tế.
- **Việc cần làm tiếp theo:** Bước 7b — chọn nhiều máy (checkbox + "Chọn tất cả"), nút "Bố trí tất cả máy chạy"/"Đóng tất cả máy" (toàn tuần, khác 2 nút Chủ nhật đã có), sao chép kế hoạch (ngày này → ngày khác), ô tìm máy theo mã/tên, bộ lọc "Chỉ xem máy đang đóng". Ý tưởng "tổ trưởng báo dừng máy hộ công nhân" đã hoãn sang Đợt 3 riêng (mục 7c của `TASK_KE_HOACH_TO_TRUONG.md`).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7a/8: thao tác nhanh Chủ nhật cho cả tổ
- **Yêu cầu thực tế từ chủ dự án:** Chủ nhật thường nghỉ, nhưng trễ đơn hàng thì làm cả ca ngày lẫn ca đêm — cần thao tác nhanh cho TOÀN BỘ máy thay vì bấm từng máy một.
- **Thêm:** 2 nút "🌙 Đóng Chủ nhật" / "☀️ Bố trí chạy Chủ nhật" phía trên danh sách máy, áp dụng cho tất cả máy của tổ trong MỘT lượt. Đóng Chủ nhật mở modal riêng (chọn ca + lý do + ghi chú) rồi ghi đè đúng slot Chủ nhật của từng máy — dùng đúng cơ chế cộng dồn theo khoá `ngày|ca` như modal 1 máy, không đụng các ngày khác đã khai riêng. Bố trí chạy Chủ nhật xoá mọi ngoại lệ Chủ nhật của mọi máy (có `confirm()` cảnh báo trước vì ảnh hưởng toàn bộ máy cùng lúc), giữ nguyên các ngày khác.
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 506 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, thử 2 nút Chủ nhật + luồng cộng dồn (T2-T6 rồi Sửa thêm CN) trên cùng 1 tuần, Lưu, F5 kiểm tra.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7a/8: "Sửa" giờ CỘNG DỒN thay vì ghi đè
- **Câu hỏi thực tế từ chủ dự án:** đóng máy T2-T6 chỉ ca ngày, nhưng Chủ nhật tổ không sản xuất thì khai kiểu gì? Trả lời: khai làm 2 lượt (T2-T6 ca ngày, rồi "Sửa" thêm Chủ nhật cả 2 ca) — nhưng code cũ mỗi lần Xác nhận **thay hẳn** toàn bộ ngoại lệ của máy, nên lượt 2 sẽ xoá mất lượt 1.
- **Sửa:** Chế độ **"Cả tuần"** vẫn thay hẳn (khai lại từ đầu). Chế độ **"Chọn ngày cụ thể"** giờ chỉ **ghi đè đúng (ngày, ca) vừa chọn**, giữ nguyên mọi (ngày, ca) khác đã khai từ lượt "Sửa" trước — gộp theo khoá `ngày|ca`. Thêm chú thích ngắn trong modal giải thích hành vi này.
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 434 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, thử đúng kịch bản: đóng T2-T6 ca ngày (lượt 1) → Sửa thêm Chủ nhật cả 2 ca (lượt 2) → xác nhận cả 7 lượt còn nguyên → Lưu → F5 kiểm tra.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7a/8: sửa modal cho chọn NHIỀU ngày
- **Phát hiện lúc test tay:** Chủ dự án thử modal đóng máy, phát hiện "Một ngày" và "Một ngày, một ca" chỉ cho chọn **đúng 1 ngày** (chip hoạt động kiểu radio) — thực tế cần đóng máy vào vài ngày cụ thể cùng lúc (ví dụ thứ Tư và thứ Sáu), không phải cả tuần.
- **Sửa:** Gộp "Một ngày"/"Một ngày, một ca" thành một chế độ **"Chọn ngày cụ thể"** — chip ngày giờ bấm được NHIỀU ngày (toggle độc lập, không còn radio), kèm 1 bộ chọn ca áp dụng chung cho các ngày đã chọn (**Cả 2 ca** / Ca ngày / Ca đêm — ẩn Ca đêm nếu tổ không có). "Áp dụng" giờ chỉ còn 2 lựa chọn: Cả tuần / Chọn ngày cụ thể.
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 427 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, thử lại modal — chọn được nhiều ngày cùng lúc, Lưu, F5 kiểm tra đúng dữ liệu.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 7a/8: đóng/mở từng máy + lưu kế hoạch tuần
- **Thay đổi:** Thêm modal đóng máy vào `ToTruong.html` (áp dụng: cả tuần / một ngày / một ngày-một ca, chọn lý do từ `Cau_Hinh.LY_DO_DONG_MAY_KE_HOACH`, ghi chú bắt buộc nếu "Khác"). Nút "Bố trí lại" xoá hết ngoại lệ của một máy. Nút "Lưu kế hoạch tuần" cố định đáy màn hình, gộp toàn bộ thay đổi thành 1 lượt gọi `luuKeHoachTuan`.
- **Mô hình state client:** `NGOAI_LE` (map theo máy) chỉ được nạp từ server lúc `layKeHoachTuan` trả về; mọi thao tác đóng/mở/sửa chỉ sửa biến này trong bộ nhớ, KHÔNG gọi RPC — đúng nguyên tắc "tổ trưởng thao tác nhiều lần, chỉ 1 lượt ghi lên Sheet" của kế hoạch gốc. Đổi tuần mà còn thay đổi chưa lưu → `confirm()` cảnh báo trước khi bỏ.
- Thêm `lyDoDongMay: dsLyDoDongMayKeHoach_()` vào `getToTruongBootstrap` để form có danh sách lý do mà không cần thêm RPC riêng.
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 419 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, thử luồng thật: đóng máy theo cả 3 kiểu áp dụng, Lưu, F5 kiểm tra đúng dữ liệu, thử "Bố trí lại". Sau đó mới sang bước 7b (thao tác hàng loạt thật sự).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 6/8: ĐÃ XÁC NHẬN cả 3 ca, bước 6 hoàn tất
- **Thay đổi:** Không sửa mã. Chủ dự án xác nhận qua link Test deployment thật:
  1. Tổ `DET` (đã có lịch + kế hoạch test cũ) → vào thẳng màn hình chính, không còn lỗi thoáng qua sau bản vá `setTimeout`.
  2. Tổ `CMTX` (chưa khai lịch) → đúng hiện màn hình "Khai giờ làm việc lần đầu".
  3. Bấm "Lưu giờ làm việc" ở màn hình đó → chuyển đúng sang màn hình chính, 38 máy CMTX đều "Bố trí chạy cả tuần" (mặc định đúng thiết kế "chỉ lưu ngoại lệ").
- **Trạng thái:** Bước 6 hoàn tất, xác nhận chạy thật đúng trên cả 2 nhánh (đã khai / chưa khai) + luồng lưu lần đầu.
- **Việc cần làm tiếp theo:** Bước 7 — thao tác hàng loạt trong `ToTruong.html`: chọn nhiều máy, đóng máy (modal chọn lý do + ghi chú), bố trí/đóng tất cả, sao chép kế hoạch, tìm máy, lọc "chỉ xem máy đang đóng".

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 6/8: vá lỗi thoáng qua khi tải trang lần đầu
- **Phát hiện lúc test tay:** Mở link `page=kehoach&to=DET&token=test123` lần đầu, `getToTruongBootstrap` chạy đúng (hiện "TỔ DET") nhưng `layKeHoachTuan` báo `Không tìm thấy hàm tập lệnh: layKeHoachTuan` — dù hàm có thật trong code đã push (xác nhận lại bằng grep). Bấm nút "Tuần này" (gọi lại đúng `layKeHoachTuan`) ngay sau đó thì chạy đúng.
- **Chẩn đoán:** Không phải thiếu hàm — là lỗi thoáng qua của `google.script.run` khi gọi RPC thứ hai (`layKeHoachTuan`) NGAY trong callback thành công của RPC thứ nhất (`getToTruongBootstrap`), cầu nối iframe của Apps Script đôi khi chưa kịp sẵn sàng ngay sau khi trang vừa tải.
- **Vá:** `nhanBootstrap` lùi lệnh gọi `veManChinh()` một nhịp bằng `setTimeout(..., 100)`. Thêm `taiKeHoachTuan_(tuan, choThuLai)` — tự thử lại đúng 1 lần sau 400ms nếu lần đầu lỗi, người dùng không cần biết để tự bấm lại. Có chặn kết quả trả về muộn của tuần cũ nếu người dùng đã kịp đổi sang tuần khác trong lúc chờ (so `tuan === TUAN_BAT_DAU` trước khi vẽ).
- **`kiem-tra.ps1` sạch cả 5 lớp** (`ToTruong.html` 244 dòng).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, tải lại trang tổ `DET` vài lần xem còn lỗi thoáng qua không, và vẫn cần thử nhánh "tổ chưa khai lịch" (chưa xác nhận ở lượt trước).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 6/8: `ToTruong.html` khung sườn thật
- **Thay đổi:** Viết lại toàn bộ `ToTruong.html` (228 dòng): màn hình khai giờ làm việc lần đầu (hiện khi `getToTruongBootstrap().lich === null`, dùng `<input type="time">`, gọi `luuLichLamViec`) → màn hình chính (chọn tuần bằng 3 nút Tuần trước/Tuần này/Tuần sau tính theo thứ Hai, gọi `layKeHoachTuan` mỗi lần đổi tuần, thống kê nhanh N máy/X bố trí chạy/Y đóng, danh sách máy — mỗi máy liệt kê chi tiết ngoại lệ đóng máy theo `Thứ + ngày + ca + lý do` nếu có). Dùng lại toàn bộ class có sẵn trong `Style.html` (`.the`, `.nut`, `.bao`, `.phu-kin`...) — không sửa `Style.html`.
- **Phạm vi cố ý CHƯA làm** (để bước 7): chưa có nút bấm sửa từng máy, chưa có modal chọn lý do đóng máy, chưa có thao tác hàng loạt (bố trí tất cả/đóng tất cả/chọn nhiều máy/sao chép tuần), chưa có UI "đổi giờ làm việc" sau lần khai đầu tiên (backend `luuLichLamViec` đã hỗ trợ sẵn, chỉ thiếu nút gọi lại).
- **`kiem-tra.ps1` sạch cả 5 lớp** — lớp 3 xác nhận HTML không lỗi cú pháp JS, không id lạ.
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, mở link Test deployment thử cả hai nhánh (tổ chưa khai lịch / tổ đã có dữ liệu test từ bước 4-5), báo lại giao diện có đúng không trước khi sang bước 7.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 5/8: ĐÃ XÁC NHẬN, thay hẳn đúng thiết kế
- **Thay đổi:** Không sửa mã. Chủ dự án đã `clasp push`, test tay qua Apps Script Editor: lưu 2 ngoại lệ `4T-08` (21/09, 22/09) → đọc lại đúng 2 dòng + `daKhai:true` → lưu lại với 1 ngoại lệ khác (`4T-12`, 23/09, ca đêm, request khác) → đọc lại **chỉ còn đúng 1 dòng `4T-12`**, 2 dòng `4T-08` cũ biến mất hoàn toàn. Sheet `Ke_Hoach_May` chỉ còn đúng 2 dòng thật (1 ngoại lệ + 1 `DA_KHAI`), không có dòng rác — `soDongCoDuLieu_` + cơ chế xoá-rồi-ghi-lại hoạt động đúng thiết kế.
- **Trạng thái:** Bước 5 hoàn tất, xác nhận chạy thật đúng. Đã xác nhận luôn phần "chỉ lưu ngoại lệ" hoạt động chính xác — máy không có dòng nào tự hiểu là "Bố trí chạy" (chưa test qua UI vì chưa có, nhưng logic RPC đã đúng).
- **Việc cần làm tiếp theo:** Bước 6 — `ToTruong.html` khung sườn thật: hiển thị danh sách máy của tổ, chọn tuần. Bước 7 mới tới thao tác hàng loạt.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 5/8: RPC đọc/ghi kế hoạch tuần
- **Thay đổi:** Thêm vào `KeHoachTo.gs`: `layKeHoachTuan(boPhan, token, tuanBatDau)` (đọc ngoại lệ + cờ `daKhai`) và `luuKeHoachTuan(boPhan, token, payload)` (ghi — đọc toàn bộ `Ke_Hoach_May`, giữ nguyên mọi dòng của tổ/tuần khác, thay hết ngoại lệ của đúng `Bo_Phan+Tuan_Bat_Dau` bằng dữ liệu mới + 1 dòng `DA_KHAI`, đúng khuôn đọc-sửa-ghi-1-lần của `dongBoDanhMucBoPhan_` trong `DanhMuc.gs`). Bốn hàm thuần mới: `ngayTrongTuan_`, `chuanHoaNgoaiLe_` (validate 1 ngoại lệ — máy thuộc đúng bộ phận, ngày trong tuần, ca hợp lệ, bắt buộc lý do, "Khác" bắt buộc ghi chú), `chuanHoaDanhSachNgoaiLe_` (validate cả danh sách, chặn khai trùng ngày+ca+máy trong cùng 1 lần lưu), `tachDuLieuKeHoachTuan_` (tách giữ lại / bỏ đi theo đúng tổ+tuần, tìm `Request_ID` cũ chống double-tap).
- Thêm khoá `Cau_Hinh.LY_DO_DONG_MAY_KE_HOACH` (danh sách lý do đóng máy theo kế hoạch, sửa trên Sheet không cần deploy) — khác `LY_DO_DUNG_MAY` (lý do công nhân báo dừng máy tức thời).
- **Sửa lại khoá upsert đã ghi sai ở lượt trước** — [`TASK_KE_HOACH_TO_TRUONG.md`](../TASK_KE_HOACH_TO_TRUONG.md) mục 5 trước đó ghi `Tuan_Bat_Dau + Ca + Ma_May`, thiếu `Ngay`. Sửa thành `Tuan_Bat_Dau + Ngay + Ca + Ma_May` — cần đủ 4 phần mới phân biệt được máy chỉ đóng một ngày (ví dụ `4T-12` chỉ thứ Tư ca ngày, mục 5 `plan18.9.md`).
- Ghi thêm dùng đúng `soDongCoDuLieu_` (không lặp lại bẫy `getLastRow()` của bước 4), neo theo cột `Tuan_Bat_Dau` (mọi dòng thật, kể cả `DA_KHAI`, đều có cột này).
- Thêm 18 test mới vào `Test.gs`.
- **`kiem-tra.ps1` sạch cả 5 lớp.**
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, chạy menu 🧪 (kỳ vọng **397** = 379 + 18), rồi test tay RPC ghi/đọc kế hoạch tuần qua Apps Script Editor (chưa có UI). Sau đó sang bước 6-7 (`ToTruong.html` thật).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 4/8: ĐÃ XÁC NHẬN bản vá đúng
- **Thay đổi:** Không sửa mã. Chủ dự án đã `clasp push`, chạy lại `setupSystem()`, xoá 2 dòng rác ở 1001/1002 trong `Lich_Lam_Viec_To`, chạy lại test RPC ghi — lần này 2 dòng nằm đúng ở dòng **2 và 3**, dòng `2026-09-01` vẫn giữ nguyên `12:00–13:00` sau khi lưu thêm dòng `2026-10-01`. Bản vá `soDongCoDuLieu_` hoạt động đúng như thiết kế.
- **Trạng thái:** Bước 4 hoàn tất, xác nhận chạy thật đúng.
- **Việc cần làm tiếp theo:** Bước 5 — RPC đọc/ghi kế hoạch máy theo tuần (`Ke_Hoach_May`: ngoại lệ, upsert, chống double-tap, thao tác hàng loạt 50–60 máy).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 4/8: vá lỗi `getLastRow()` bị checkbox làm sai
- **Phát hiện lúc test tay:** `luuLichLamViec` ghi 2 dòng vào `Lich_Lam_Viec_To` đều báo `ok:true`, nhưng dữ liệu nằm ở dòng **1001/1002**, không phải dòng 2/3 như mong đợi. `Ctrl+End` xác nhận đúng vậy.
- **Nguyên nhân:** `datCheckbox_(sh, cot)` áp data validation checkbox lên tới `sh.getMaxRows()-1` dòng (~999 dòng). Google Sheets coi các ô checkbox còn trống trong vùng đó là `FALSE`, khiến `sh.getLastRow()` báo có dữ liệu tới dòng ~1000 dù sheet trông như trống. `luuLichLamViec` dùng `getLastRow()+1` để tìm dòng trống kế tiếp → ghi lạc. Cùng lý do, đoạn seed `Danh_Muc_To` kiểu "chỉ chạy khi `getLastRow() < 2`" không bao giờ chạy — 9 bộ phận còn lại (SOI, TRANG, CMTX, CMTD, MTX, CO, ICM, LT, CHUNG) chưa từng được thêm, chỉ có dòng `DET` do chủ dự án tự gõ tay lúc test bước 2.
- **Vá:** thêm `soDongCoDuLieu_(sh, cotNeo)` vào `Code.gs` — đếm dòng dữ liệu thật theo MỘT cột neo cụ thể, không tin `getLastRow()` của cả sheet. Dùng hàm này trong `luuLichLamViec` (thay `getLastRow()`) và đổi `Danh_Muc_To` sang kiểu **bổ sung bộ phận còn thiếu** (giống `CAU_HINH_MAC_DINH`) thay vì "chỉ seed khi trống hẳn" — không phá dòng `DET` đã gõ tay, vẫn thêm được 9 bộ phận còn lại. Chi tiết đầy đủ ở [`TASK_KE_HOACH_TO_TRUONG.md`](../TASK_KE_HOACH_TO_TRUONG.md) mục 7b.
- **⚠️ Nghi ngờ CÙNG LỖI tồn tại từ trước trong `DanhMuc.gs` (`themMayMoi()`), ngoài phạm vi tính năng này — CHƯA sửa, cần chủ dự án xác nhận riêng.** Có thể 2 máy `CMTD02`/`CMTD03` đang nằm ở dòng ~1000 của `Danh_Muc_May` thay vì gần các máy khác — không hỏng chức năng (mọi chỗ đọc qua `docSheet_` tự lọc, không phụ thuộc vị trí dòng), chỉ khó tìm bằng mắt.
- **`kiem-tra.ps1` sạch cả 5 lớp.**
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push` → chạy lại `setupSystem()` (thêm 9 bộ phận thiếu vào `Danh_Muc_To`) → xoá 2 dòng rác ở 1001/1002 trong `Lich_Lam_Viec_To` → chạy lại đoạn test RPC ghi, xác nhận lần này ra đúng dòng 2/3.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 4/8: RPC ghi lịch làm việc
- **Thay đổi:** Thêm vào `KeHoachTo.gs`: `chuanHoaPayloadLichTo_` (validate payload + build dòng theo `HEADER_LICH_TO`, hàm thuần), `timDongLichTrungApDung_` (tìm dòng cùng `Bo_Phan+Ap_Dung_Tu` để sửa đè, `-1` = thêm dòng mới — đây là chỗ bảo đảm "đổi lịch không ghi đè lịch cũ", hàm thuần), và RPC `luuLichLamViec(boPhan, token, payload)`. Khoá upsert tự nhiên là `(Bo_Phan, Ap_Dung_Tu)`: bấm Lưu 2 lần cùng ngày áp dụng chỉ ghi đè đúng dòng đó (idempotent), không cần thêm cột `Request_ID` cho `Lich_Lam_Viec_To` như đã làm với `Ke_Hoach_May`.
- `bp` (bộ phận ghi vào sheet) luôn lấy từ **bản ghi đã xác thực** `to.Bo_Phan`, không tin tham số `boPhan` client gửi lên — chặn sửa tham số URL để ghi vào bộ phận khác.
- Thêm 9 test mới vào `Test.gs` cho 2 hàm thuần trên.
- **`kiem-tra.ps1` sạch cả 5 lớp.**
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, chạy menu 🧪 trong Sheet (kỳ vọng **379** = 370 + 9, không đỏ). Test tay RPC ghi thật (chưa có UI, gọi trực tiếp qua Apps Script Editor hoặc chờ bước 6-7 có form) trước khi sang bước 5.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 3/8: ĐÃ XÁC NHẬN — 370/370, không đỏ
- **Thay đổi:** Không sửa mã. Chủ dự án đã `clasp push`, chạy menu 🧪 "Chạy test logic" trong Sheet, ra **370/370**, không có test nào hỏng.
- **Con số không khớp dự đoán 352 — đã tìm ra lý do, không phải lỗi:** `bao-tri-v2/CLAUDE.md` mục 14b ghi "340/340" từ 11/09, nhưng lần "kéo về repo" 18/09 đã kéo thêm các test lưu trữ phiếu theo tháng lịch (`mocThangLuuTru_`, `chonPhieuLuuTru_`...) từ Apps Script Editor về mà tài liệu chưa cập nhật số — nền thật trước khi thêm 12 test của bước này đã là 358, không phải 340. Sẽ sửa lại con số trong `CLAUDE.md` ở bước 8 (khi cập nhật tài liệu cho toàn bộ tính năng).
- **Trạng thái:** Bước 3 hoàn tất, xác nhận chạy thật đúng.
- **Việc cần làm tiếp theo:** Bước 4 — RPC ghi: lưu lịch làm việc của tổ (`Lich_Lam_Viec_To`, có `Ap_Dung_Tu`, không ghi đè lịch cũ khi đổi).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 3/8: RPC đọc bootstrap (KeHoachTo.gs)
- **Thay đổi:** Thêm vào `KeHoachTo.gs`: `dsMayCuaBoPhan_` (lọc máy theo bộ phận + `Hoat_Dong=TRUE`), `chuanHoaNgay_` + `lichHienHanhCuaTo_` (chọn dòng `Lich_Lam_Viec_To` có `Ap_Dung_Tu` lớn nhất mà vẫn ≤ ngày cần tra, trả `null` nếu tổ chưa khai lịch), và RPC `getToTruongBootstrap(boPhan, token)`. **Gộp một RPC duy nhất** thay vì 3 RPC riêng như phác thảo ban đầu trong `TASK_KE_HOACH_TO_TRUONG.md` — đúng lối `getWorkerBootstrap()` đã dùng cho trang công nhân, giảm round-trip `google.script.run`. `xacThucTo_` cũng được sửa thêm tham số `dsTo` tiêm được, đúng lối `getOnDutyContacts_`.
- Thêm 12 test mới vào `Test.gs` (mục "TỔ TRƯỞNG KHAI KẾ HOẠCH MÁY — bước 3/8"), test cả ba hàm bằng dữ liệu giả, không đụng sheet nào.
- **`kiem-tra.ps1` sạch cả 5 lớp.** Lớp 6 (340 test cũ) chỉ chạy được trong Sheet, chưa xác nhận — cần push rồi chạy menu 🧪.
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`, rồi chạy menu 🧪 "Chạy test logic" trong Sheet — kỳ vọng **352/352** (340 cũ + 12 mới). Sau đó sang bước 4 (RPC ghi: lưu lịch làm việc).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 2/8: ĐÃ XÁC NHẬN chạy thật qua Test deployments
- **Thay đổi:** Không sửa mã. Chủ dự án đã điền tay `Token=test123` cho dòng `DET` trong `Danh_Muc_To`, mở URL "Triển khai thử nghiệm" (`.../exec?page=kehoach&to=DET&token=...` — deployment test riêng, KHÔNG phải bản `/exec` đang phục vụ 162 máy thật) và xác nhận cả hai ca: token đúng → "Kế hoạch tuần — DET" (đúng cả nội dung lẫn tiêu đề tab), token sai → "Không có quyền truy cập".
- **Trạng thái:** `xacThucTo_` + route `page=kehoach` đã chạy thật đúng như thiết kế. Vẫn **CHƯA deploy** bản `/exec` chính thức — bước này không cần, vì test deployment đã đủ xác nhận logic. Sẽ deploy chung một lượt khi cả tính năng hoàn chỉnh (sau bước 7-8), tránh deploy nhiều lần giữa chừng.
- **Việc cần làm tiếp theo:** Bước 3 — RPC đọc thông tin tổ + lịch làm việc hiện hành + danh sách máy của tổ, test bằng `chayTest()` (chưa cần UI).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 2/8: ĐÃ `clasp push` (24 file, 15:38)
- **Thay đổi:** Không sửa dòng mã nào thêm. Chủ dự án đã tự chạy `clasp.cmd push --force`, có `KeHoachTo.gs` và `ToTruong.html`.
- **Trạng thái:** Đã push GitHub, **ĐÃ `clasp push`** lúc 15:38, 24 file. **CHƯA deploy.** Theo đúng `bao-tri-v2/CLAUDE.md` mục 12: *"Chỉ trang web `/exec` mới phục vụ theo version đã deploy"* — nghĩa là URL `/exec` thật (QR đang dùng) sẽ **KHÔNG** thấy `page=kehoach` cho tới khi deploy. Test route mới phải dùng URL **"Triển khai thử nghiệm" (Test deployments)** trong Apps Script Editor — URL riêng luôn chạy đúng code HEAD vừa push, không đụng gì tới bản `/exec` đang phục vụ 162 máy thật, không cần deploy.
- **Việc cần làm tiếp theo:** Điền tay `Token` + tick `Hoat_Dong` cho 1 dòng `Danh_Muc_To` (vd `DET`). Lấy URL Test deployments (Apps Script Editor → Triển khai → Quản lý bản triển khai → hoặc nút "Triển khai thử nghiệm" cạnh nút Triển khai), mở `<URL đó>?page=kehoach&to=DET&token=...` để thử — không đụng bản `/exec` thật.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 2/8: xác thực + route (KeHoachTo.gs mới)
- **Thay đổi:** Thêm `KeHoachTo.gs` (file mới) với `xacThucTo_(boPhan, token)` — độc lập hoàn toàn với `xacThucTho_` của thợ, không dùng chung cột/hàm — và `renderTrangKeHoach_(p)`. Thêm `ToTruong.html` (file mới, khung rỗng: xác thực xong hiện đúng tên tổ, form thật để bước 6-7). Thêm nhánh `page === 'kehoach'` vào `doGet` trong `CongNhan.gs`, cùng chỗ với `qr`/`ngay`, và cập nhật lại docstring liệt kê route cho khớp thực tế (trước đó thiếu `qr`/`ngay`).
- **Vì `appsscript.json` đặt `access: ANYONE_ANONYMOUS`:** không có gì chặn RPC ở tầng route, nên mọi RPC ghi/đọc của tính năng này (từ bước 3 trở đi) đều phải tự gọi lại `xacThucTo_` bên trong hàm — đã ghi rõ trong docstring `renderTrangKeHoach_`.
- **`kiem-tra.ps1` sạch cả 5 lớp** (cú pháp, biến che tham số, HTML — `ToTruong.html` được nhận đúng, KPI 2042/2042, Telegram 229/229).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.**
- **Việc cần làm tiếp theo:** Xác nhận `clasp push`. Sau đó cần điền tay `Token` cho ít nhất 1 dòng trong `Danh_Muc_To` (chưa có RPC sinh token tự động ở bước này) để thử link `?page=kehoach&to=<Bo_Phan>&token=<token đã điền>` — token sai phải bị từ chối, token đúng phải hiện đúng tên tổ. Xong thì báo lại để sang bước 3 (RPC đọc thông tin tổ + lịch làm việc + danh sách máy).

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 1/8: ĐÃ `clasp push`, chưa chạy `setupSystem()`
- **Thay đổi:** Không sửa dòng mã nào thêm. Mục ghi **thay đổi trạng thái** của mục ngay bên dưới: chủ dự án đã tự chạy `clasp.cmd push --force` trong PowerShell của mình (lệnh này bị chặn ở tầng permission của phiên làm việc, đúng thiết kế — chủ dự án phải tự chạy).
- **Trạng thái:** Đã push GitHub, **ĐÃ `clasp push`** lúc 15:32, 22 file. **CHƯA deploy** — không cần, vì hàm chạy từ menu Sheet dùng thẳng code HEAD vừa push, không phải trang `/exec`. **CHƯA chạy `setupSystem()`** trong Sheet thật, nên 3 sheet mới (`Danh_Muc_To`, `Lich_Lam_Viec_To`, `Ke_Hoach_May`) **chưa tồn tại** trên Sheet — chỉ mới có trong code.
- **Việc cần làm tiếp theo:** Mở Google Sheet → menu 🔧 Bảo trì → "1. Cài đặt hệ thống" → chạy, xem thông báo tạo/cập nhật sheet có liệt kê đủ 3 sheet mới không, rồi mở từng sheet kiểm tra đúng cột như `TASK_KE_HOACH_TO_TRUONG.md` mục 3 đã mô tả. Xong thì báo lại để sang bước 2.

## [2026-09-18] Tổ trưởng khai kế hoạch máy — Bước 1/8: schema 3 sheet mới trong Code.gs
- **Thay đổi:** Thêm `SHEET.TO/LICH_TO/KE_HOACH_MAY` + `HEADER_TO/HEADER_LICH_TO/HEADER_KE_HOACH_MAY` + `TRANG_THAI_KE_HOACH_MAY` vào `bao-tri-v2/Code.gs`, và đoạn tạo 3 sheet (`Danh_Muc_To`, `Lich_Lam_Viec_To`, `Ke_Hoach_May`) vào `setupSystem()`, chèn giữa khối `Khung_Ngung_Ke_Hoach` và `Cau_Hinh`. Thiết kế đầy đủ, lý do, và toàn bộ lộ trình 8 bước nằm ở [`TASK_KE_HOACH_TO_TRUONG.md`](../TASK_KE_HOACH_TO_TRUONG.md) — đọc file đó trước khi làm tiếp bước 2.
- **Điểm quan trọng cần nhớ:** `Ke_Hoach_May` chỉ lưu NGOẠI LỆ (máy Đóng máy), không lưu đủ mọi tổ hợp máy×ngày×ca. `Lich_Lam_Viec_To` là nguồn giờ ca THỨ HAI, song song với `Ke_Hoach_Chay_May` đã có — không tự đồng bộ với nhau, không đụng gì tới `HieuDung.gs` (vẫn giữ nguyên, đang tắt).
- **`kiem-tra.ps1` sạch cả 5 lớp** (cú pháp, biến che tham số, HTML, KPI đáp ứng 2042/2042, tin Telegram 229/229) — chưa thêm test riêng cho tính năng mới (để dành bước 8).
- **Trạng thái:** Đã sửa trong thư mục làm việc, **chưa commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, CHƯA `clasp push`, CHƯA deploy.** Ba sheet mới chỉ tồn tại trong mã nguồn, chưa chạy thật trong Sheet — `setupSystem()` cần `clasp push` mới có hiệu lực, đang chờ xác nhận riêng.
- **Việc cần làm tiếp theo:** Xác nhận `clasp push` để chạy thử `setupSystem()` trong Sheet thật, xem 3 sheet mới đúng cột, không đụng sheet cũ. Sau đó mới sang bước 2 (`xacThucTo_` + route `page=kehoach`).

## [2026-09-18] Kéo về repo 5 file bị sửa thẳng trên Apps Script Editor (lưu trữ phiếu + 2 bản vá lỗi KPI thật)
- **Thay đổi:** `dongbo.ps1` phát hiện `BaoCao.gs`, `Code.gs`, `CongNhan.gs`, `LuongTho.gs`, `Test.gs` lệch giữa repo và bản đang chạy trên Apps Script (Apps Script nhiều hơn 97/73/2/193/91 dòng). So diff đầy đủ (không chỉ đếm dòng) cho thấy Apps Script Editor **mới hơn** repo, có người đã sửa thẳng trên Editor mà chưa bao giờ đẩy về Git — đúng kiểu sự cố "repo tụt ~1000 dòng" đã ghi nhận trước đây. Đã copy nguyên văn 5 file từ bản Apps Script Editor (`clasp clone` vào thư mục tạm, chỉ đọc) đè vào repo, không sửa tay dòng nào.
- **Nội dung phần bị thiếu trong repo trước khi kéo về:**
  1. **Tính năng lưu trữ phiếu theo tháng lịch** — `mocThangLuuTru_()` cắt theo tháng lịch (không phải trượt N ngày), `chonPhieuLuuTru_()` giữ lại phiếu chưa tính KPI trước khi dọn, `donPhieuCuHangThang()` (tính KPI trước, dọn sau) thay cho gọi thẳng `archiveOldTickets()` trong trigger. `Cau_Hinh.SO_THANG_GIU_LAI` đổi nghĩa từ "số tháng trượt, mặc định 12" (bản repo cũ) sang "số tháng lịch giữ lại, mặc định 1" (bản Apps Script).
  2. **Bản vá lỗi thật 12/09/2026 — dropdown ghi đè âm thầm cột KPI:** có người tạo tay dropdown `Loai_Phieu` (bản cũ, thiếu `HO_TRO`) nhưng đặt nhầm lên cột `KPI_Ap_Dung`, khiến `setValues()` báo ghi xong nhưng Google âm thầm huỷ lệnh ghi — `tinhLaiKpiTho()` chạy nhiều ngày liền vô ích không ai biết. Vá bằng `goQuyTacXacThuc_()` gỡ hết validation khỏi các cột do máy tự ghi (`COT_MA_TU_GHI`), gọi trong `setupSystem()`.
  3. **Bản vá lỗi thật 12/09/2026 — thứ tự tính-rồi-dọn:** nút 🗄️ gọi `tinhLaiKpiTho()` rồi `archiveOldTickets()` liền trong cùng lượt, phần dọn đọc lại sheet trước khi Apps Script đẩy lệnh ghi KPI xuống thật sự nên luôn báo "còn phiếu chưa tính KPI" dù dòng trên vừa báo ghi xong. Vá bằng `SpreadsheetApp.flush()` sau khi ghi KPI.
  4. Thêm hàm chẩn đoán `thongKeKpi_()` (chỉ đọc, không đổi hành vi) và cải thiện thông báo lỗi của `tinhLaiKpiTho()` (mỗi dòng lỗi nhận đúng lý do, không kéo sập cả lượt tính).
- **`kiemtra\kiem-tra.ps1` chạy sạch sau khi kéo về:** cú pháp `.gs` OK, không biến che tham số, HTML sạch, **KPI 2042/2042** (tăng từ 2039 vì có thêm test lưu trữ), nội dung tin Telegram 229/229.
- **Trạng thái:** Đã sửa trong thư mục làm việc, **CHƯA commit** lúc viết dòng này (commit ngay sau). **CHƯA push GitHub, KHÔNG `clasp push`, KHÔNG deploy.** Vì đây là kéo dữ liệu từ Apps Script VỀ repo (cùng chiều với Apps Script đang chạy), không phải đẩy code MỚI lên — hệ thống đang phục vụ 162 máy không hề bị chạm.
- **Việc cần làm tiếp theo:** `git push` khi chủ dự án sẵn sàng (hoặc để `roi-may.ps1` lo lúc rời máy). Sau đó chạy lại `dongbo.ps1` để xác nhận repo đã khớp cả GitHub lẫn Apps Script — không còn lệch. Không cần `clasp push` vì Apps Script vốn đã là bản mới nhất; mục đích lần này chỉ là đưa Git bắt kịp thực tế.

## [2026-09-11] ĐÃ DEPLOY phiếu `HT-`. Chạy thật thành công trên máy 4T-08
- **Thay đổi:** Không sửa dòng mã nào. Mục ghi **thay đổi trạng thái**: tính năng gọi kỹ thuật nay đang phục vụ thật, cộng vài chỗ cập nhật trạng thái trong `bao-tri-v2/CLAUDE.md`.
- **Trạng thái:** Đã push GitHub, **ĐÃ `clasp push`** lúc 09:07, **ĐÃ DEPLOY** — version 6 lên version mới, **cùng deployment ID cũ**, URL `/exec` không đổi nên 175 QR đã in vẫn sống. Khoá `LY_DO_DUNG_MAY` đã thêm `Đổi mặt hàng`, sửa thẳng trên sheet `Cau_Hinh`, không cần deploy lại.
- **Chạy thật một vòng đầy đủ, đây là mốc đã kiểm chứng của hôm nay.** Máy `4T-08` (Máy 08 dệt 4 thoi, DET): `DM-1109-001` lý do *Đổi mặt hàng* lúc 09:25 → quét lại QR, màn hình hiện đúng cả hai phần: thẻ cam "Máy này đang dừng" với nút Máy đã chạy lại, và thẻ "Cần thợ kỹ thuật tới máy?" → bấm gọi, ra `HT-1109-001` lúc 09:26, danh bạ đúng 3 thợ cơ khí ca ngày cộng số khẩn cấp.
- **`clasp deploy` không chạy được, phải deploy bằng Editor.** Lỗi *Only users in the same domain as the script owner may deploy this script* — tài khoản clasp đang đăng nhập không cùng domain Workspace với chủ sở hữu script, đúng cái bẫy tài liệu đã ghi từ 09/09. Đường vòng: **Triển khai → Quản lý bản triển khai → chọn bản đang có → bút chì → Phiên bản mới → Triển khai**. Giữ nguyên deployment ID, và **không bao giờ bấm "Bản triển khai mới"**. Nếu sau này muốn dùng lại `clasp deploy` thì phải đăng nhập lại bằng tài khoản cùng domain với chủ sở hữu.
- **Hai khối của 09/09 cũng lên theo.** Deploy lấy trọn mã HEAD chứ không lấy riêng phần mới, nên *so sánh với kỳ trước* trong báo cáo xuất và *tính lại KPI đáp ứng của thợ* nay cũng đang chạy thật. Bước 7 mục Telegram trong `bao-tri-v2/CLAUDE.md` cũng coi như xong — phần còn thiếu để bot chạy thật chỉ còn công tắc `TELEGRAM_BAT` và việc ghép chat id.
- **Đã soạn tin Zalo báo thợ dệt**, có ba điểm chủ dự án bổ sung: dùng đúng chữ *thợ dệt* và *các anh cơ khí*; bấm nút xong **vẫn phải** gọi điện hoặc tới báo tận nơi, nút chỉ để ghi nhận giờ và lưu phiếu; và nhắc các anh cơ khí **phải bấm Hoàn thành** phiếu `HT-` vì thợ dệt bật máy chạy lại không đóng phiếu đó.
- **Một câu hỏi đã trả lời, nên ghi lại:** xoá phiếu `HT-` **không** làm mất phiếu `DM-`, hai dòng riêng trong `Su_Co`, chỉ nối nhau bằng chữ trong cột mô tả. App cũng không có nút xoá `HT-` — chỉ `BT-` mới xoá được từ trang thợ. Gọi nhầm thì nên để thợ nhận rồi bấm Hoàn thành ghi "gọi nhầm", còn hơn xoá dòng làm mất cả giờ báo lẫn công thợ.
- **Việc cần làm tiếp theo:** Theo dõi vài ca đổi mặt hàng thật trong tuần xem thợ dệt có quét lần thứ hai không, và các anh cơ khí có bấm Hoàn thành không — đây là hai chỗ dễ rơi nhất, và cả hai đều hỏng im lặng. Phần Telegram không đổi: ghép đủ chat id, xoá dãy số mượn tạm, rồi mới gõ `BAT`.

## [2026-09-11] Phiếu `HT-` — ĐÃ `clasp push` 22 file. Chưa deploy. Test trong Sheet 340/340
- **Thay đổi:** Không sửa dòng mã nào. Đây là mục ghi **thay đổi trạng thái** của mục phiếu `HT-` bên dưới, cộng hai chỗ sửa số trong `bao-tri-v2/CLAUDE.md` (`~270` thành `340`).
- **Trạng thái:** **ĐÃ `clasp push`** lúc 09:07 ngày 11/09/2026, 22 file, có `CongNhan.gs` và `Index.html`. **CHƯA deploy.** Dùng `--force`, và lần này `--force` có căn cứ chứ không phải tin nhau: `dongbo.ps1` chạy ngay trước đó liệt kê đúng 12 file lệch, **cả 12 đều repo nhiều dòng hơn server**, không file lạ nào, nên không ai sửa thẳng trên Editor. Chủ dự án xác nhận riêng cho lần push này.
- **Mục 14b đã chạy thật trong Sheet: 340/340 đạt.** Trước đó chỉ có kết quả từ khung tạm chạy bằng node (18/18). Con số tổng 340 lệch với chỗ tài liệu ghi `~250` từ trước — đã sửa thành 340 ở cả hai chỗ trong `bao-tri-v2/CLAUDE.md`.
- **Hệ thống đang chạy chưa đổi hành vi nào.** `clasp push` chỉ cập nhật mã HEAD; công nhân quét QR vẫn vào bản đã triển khai cũ, **chưa thấy nút "Gọi kỹ thuật"**. Thứ đổi ngay là menu trong Sheet và mã mà trigger chạy.
- **Hai chỗ vấp trên đường đi, ghi lại để lần sau khỏi mất thời gian.** (1) Máy công ty kẹt giữa một phiên `git am` còn sót từ lúc thử áp file patch — mọi lệnh checkout bị chặn cho tới khi `git am --abort`. (2) `clasp.cmd push` chạy ở gốc repo báo `Project settings not found`, vì `.clasp.json` của hệ v2 nằm trong `bao-tri-v2/`. **Phải `cd bao-tri-v2` trước.** Chạy ở gốc là trỏ vào Apps Script của SAPHIA — hệ khác hẳn.
- **Việc cần làm tiếp theo:** Thêm `Đổi mặt hàng` vào khoá `LY_DO_DUNG_MAY` của sheet `Cau_Hinh` — **vẫn chưa làm**, sửa trên sheet là có hiệu lực ngay. Rồi deploy vào lần chủ nhật, dùng đúng deployment ID đang có. Sau deploy đi xem một ca đổi mặt hàng thật từ đầu tới cuối.

## [2026-09-11] Bỏ chặn `git push` trong `.claude/settings.json`
- **Thay đổi:** Xoá hai dòng `"Bash(git push:*)"` và `"PowerShell(git push:*)"` khỏi mảng `deny`. Khối `deny` từ 10 dòng còn 8, **toàn bộ là `clasp`**. Không đụng `allow`, không sửa dòng mã nào.
- **Lý do:** Phiên làm việc trên đám mây không đẩy được commit lên GitHub, hai commit phiếu `HT-` kẹt lại trong container. Mà container là thứ dùng xong bỏ — kẹt ở đó là mất hẳn. Đây đúng kiểu hỏng đã trả giá hai lần: repo tụt ~1000 dòng, và 7 file kẹt trên laptop hôm 03/09.
- **Vì sao bỏ được mà không mất an toàn:** `git push` khác hẳn `clasp push` và `clasp deploy` về mức nguy hiểm. Đẩy lên một nhánh phụ trên GitHub thì không chạm gì tới Apps Script, 175 máy không hề biết, và sai thì xoá nhánh là xong. Răng nanh thật của hàng rào nằm ở tám dòng `clasp`, và tám dòng đó giữ nguyên. **Vẫn không được `clasp push` hay `clasp deploy` khi chưa có xác nhận riêng từng lần.**
- **Không viết luật hẹp hơn được.** Ý muốn tự nhiên là "cấm push chung, mở riêng nhánh `claude/*`", nhưng `deny` luôn thắng `allow` nên một dòng `allow` hẹp không bao giờ lách qua được dòng `deny` rộng. Muốn mở thì phải bỏ hẳn.
- **Vì sao phải commit chứ không sửa tay trong container:** phiên mới clone lại repo từ GitHub, nên bản sửa tay biến mất cùng container. `settings.local.json` cũng không cứu được vì `.gitignore` loại nó, container mới không bao giờ có. Đường duy nhất để phiên sau tự đẩy được là chính thay đổi này nằm trong repo — đổi lại, nó đổi luật cho cả máy công ty lẫn laptop.
- **Một chỗ cần dọn ở phía máy công ty.** Lúc sửa bằng tay, dòng `"Bash(clasp push:*)"` bị xoá nhầm thay vì hai dòng `git push` — tức là hàng rào quan trọng nhất hở ra còn thứ định mở thì vẫn đóng. Bản trên GitHub chưa bao giờ bị sửa như vậy, nên `git pull` về là lấy lại đúng.
- **Trạng thái:** Đã sửa, đã commit, đã push GitHub. **Không** `clasp push`, **không** deploy; mã nguồn không đổi nên không cần chạy lại bộ kiểm tra.
- **Việc cần làm tiếp theo:** Không đổi so với mục phiếu `HT-` bên dưới. Thêm `Đổi mặt hàng` vào khoá `LY_DO_DUNG_MAY` của sheet `Cau_Hinh`, rồi mới tới `clasp push` (cần xác nhận riêng).

## [2026-09-11] Phiếu `HT-` — công nhân gọi kỹ thuật trong lúc máy đang dừng
- **Thay đổi:** Thêm loại phiếu thứ năm `HO_TRO` (mã `HT-`). `Code.gs` thêm hằng, `loaiPhieu_` nhận loại mới, thêm `laHoTro_` và `laDoDapUng_`. `CongNhan.gs` thêm RPC `requestTechnician` + `ghepMoTaHoTro_`, và `getWorkerBootstrap` nay trả riêng `phieuDungMay` / `phieuHoTro`. `Index.html` thêm thẻ "Cần thợ kỹ thuật tới máy?" ngay dưới nút "Máy đã chạy lại". `LuongTho.gs` cho `HT-` vào KPI đáp ứng. `HieuDung.gs` và `XuatBaoCao.gs` chặn `HT-` khỏi mọi phép đo downtime. `BaoCao.gs`, `BaoCaoNgay.gs`, `TrangNgay.html`, `Tho.html` hiện loại phiếu mới. Thêm 18 ca vào `Test.gs` mục 14b và 3 ca vào `kiemtra/kpi-tho.js`.
- **Bối cảnh — bước 3 của quy trình đổi mặt hàng.** Bước 1 công nhân tự thay chỉ sợi lên dàn, bước 2 tự dẫn hướng chỉ, bước 3 phải có thợ cơ khí tới chỉnh. Màn hình "Máy này đang dừng" trước đây chỉ có đúng một nút "Máy đã chạy lại" — cố ý ẩn hết form để không mở chồng hai khoảng dừng — nên bước 3 không có đường nào trên app. Công nhân đi bộ tìm thợ, và khoảng chờ đó không để lại vết gì trong số liệu.
- **Đã cân phương án mượn phiếu `SC-` rồi bỏ.** Mượn thì xong trong một buổi và dùng lại trọn luồng đã kiểm chứng. Cái giá là mỗi lần đổi mặt hàng cộng một lần hỏng cho một cái máy không hỏng, mà đổi mặt hàng diễn ra nhiều lần mỗi ngày và "số lần hỏng" đúng là con số báo cáo này tồn tại để đo. Chủ dự án chọn loại phiếu riêng sau khi nghe cả hai cái giá.
- **Chỗ nguy nhất là downtime đếm hai lần.** Phiếu `HT-` ghi `Trang_Thai_May = DA_DUNG` vì máy đang dừng thật, nên các hàm đo downtime rất dễ ăn nhầm. Đã chặn ở ba chỗ: `khoangDungCuaPhieu_`, `mocBatDauHu_`, `khoangDungMay_`; cột `Thoi_Gian_Dung_May` cố ý để trống. Phép hợp nhất khoảng theo máy vốn đã chặn phần chồng nhau, nhưng thợ đóng `HT-` muộn hơn lúc máy chạy lại thì cái đuôi thừa vẫn thành máy dừng oan.
- **Một bẫy im lặng đã bắt được khi đọc lại mã:** `gomTheoMayTho_` và `duLieuBaoCaoNgay_` đếm loại phiếu bằng chuỗi `if/else if` mà **nhánh cuối là bảo trì** — thêm loại mới mà quên nhánh riêng là `HT-` bị đếm thành bảo trì, không lỗi, không dấu vết. Đã thêm nhánh và có hẳn ca test tên "không bị đếm nhầm thành bảo trì". Cùng loại: `SO_COT_TONG_HOP` cắt mọi dòng của `Tong_Hop` đúng 9 cột, thêm cột mà quên nâng hằng số là cột mới biến mất lặng lẽ — đã nâng lên 10.
- **Ba quyết định của chủ dự án, chốt 11/09/2026.** (1) Đáp ứng **tính chung** một con số với sự cố — cửa duy nhất là `laDoDapUng_`, muốn tách sau này chỉ sửa một chỗ. (2) Telegram + danh bạ + **có** vào vòng nhắc 5 phút: đây là cửa thứ ba vào `ThongBao.gs`, khác phiếu `DM-` vốn cố ý không có cửa nào, vì tin này đi kèm đúng một việc để làm nên không nằm trong nhóm tin suông dạy thợ lướt qua. (3) **Chỉ gọi được khi máy đang có phiếu `DM-` mở** — máy không dừng mà cần thợ thì đó là phiếu `SC-`, cho mở ở đó là mở cửa né phiếu sự cố.
- **Máy chạy lại KHÔNG đóng phiếu `HT-` theo.** Thợ tự đóng; máy chạy được và thợ làm xong là hai việc khác nhau, đóng hộ là ghi sai giờ hoàn thành của người khác. Màn hình công nhân chỉ nhắc một dòng.
- **`requestId` riêng cho nút gọi thợ.** Dùng chung với ô của luồng dừng máy là hỏng: phép chống double-tap dò `Request_ID_Cuoi` trên mọi dòng, nên gọi thợ bằng đúng mã vừa ghi phiếu `DM-` sẽ bị coi là bấm trùng và trả lại chính phiếu đó — không ai được gọi cả. Bắt được lúc viết, chưa kịp trả giá.
- **Trạng thái:** Đã viết, **đã commit, CHƯA push GitHub** — phiên này chạy trên Linux và `git push` bị chặn quyền, lệnh push chủ dự án tự chạy. **Không** `clasp push`, **không** deploy. Công nhân quét QR vẫn vào bản cũ, chưa có nút gọi thợ. Bộ kiểm tra tại máy sạch: chốt token 46 file, chồng tham số sạch, HTML sạch, KPI **2042**/2042, nội dung tin 229/229, cú pháp `.gs` hợp lệ. Mục 14b **chưa chạy trong Sheet** vì chưa `clasp push`; đã dựng khung tạm chạy bằng node, 18/18 đạt, và kiểm chứng ngược bằng bản cố tình bỏ hai chốt chặn thì báo đúng 3 ca đỏ.
- **Chưa chạy `dongbo.ps1`** — phiên này chạy trên Linux, không có PowerShell lẫn `clasp`, nên lớp đối chiếu với Apps Script Editor chưa ai kiểm. Chạy trên máy công ty trước khi push.
- **Việc cần làm tiếp theo:** Thêm `Đổi mặt hàng` vào khoá `LY_DO_DUNG_MAY` của sheet `Cau_Hinh` — sửa trên sheet, có hiệu lực ngay, không cần deploy. Rồi `clasp push` (cần xác nhận riêng) → chạy menu 🧪 test trong Sheet để mục 14b chạy thật → deploy vào lần chủ nhật cùng các khối đang chờ. Sau deploy đi xem một ca đổi mặt hàng thật từ đầu tới cuối.

## [2026-09-10] Thêm `HUONG_DAN_BAO_DONG_THO.md` — báo động bằng MacroDroid trên máy thợ
- **Thay đổi:** Thêm file mới `HUONG_DAN_BAO_DONG_THO.md`, 104 dòng. Không sửa một dòng mã nào.
- **Bối cảnh:** Tin thử đã về máy chủ dự án thành công, nhưng chỉ kêu như thông báo bình thường. Chủ dự án muốn rung khoảng 5 giây. **Phía bot không làm được**: Telegram Bot API chỉ có công tắc bật/tắt thông báo, không có tham số điều khiển thời lượng rung, và mã đang để bật. Mọi cách đều nằm ở máy người nhận.
- **Chuỗi lọc chọn `Bấm để nhận việc`.** Đây là điểm kỹ thuật đáng nhất của tài liệu. Chuỗi này có trong tin sự cố mới và cả hai tin nhắc, nhưng **không** có trong tin `✅ ... đã nhận ... Bạn không cần xử lý`. Nghĩa là báo động kêu đúng lúc cần hành động và im khi chỉ là tin thông tin. Lọc theo tên bot thì tin "đã nhận" cũng kêu, và một báo động kêu lúc không cần làm gì là cách dạy thợ tắt nó đi.
- **Bẫy đã ghi trong tài liệu:** dòng "Bấm để nhận việc" chỉ xuất hiện khi thợ có `Link_Ca_Nhan`. Thợ chưa có link thì macro không kêu, hỏng im lặng. Phải chạy menu 4 làm mới link cá nhân trước khi phát máy.
- **Chủ dự án chọn cách 2 sau khi nghe cái giá.** Đã nêu một lần: phải cài trên từng máy, mỗi hãng chặn nền một kiểu, hỏng thì hỏng im lặng. Tài liệu vì vậy có hẳn mục 4 bảng đường đi theo hãng (Xiaomi gắt nhất và phổ biến nhất trong xưởng), và mục 5 bắt **khoá màn hình chờ ít nhất 15 phút rồi mới thử** — thử ngay sau khi cài thì luôn thấy đạt vì hệ điều hành chưa kịp giết ứng dụng.
- **Giữ lại cách nhẹ ở mục 6** (đặt âm báo dài cho riêng chat với bot, không cần app nào) vì với thợ ca ngày khu vực không ồn thì thường là đủ, và nó không có gì để hỏng.
- **Trạng thái:** Mới là tài liệu, đã commit. **Không** `clasp push`, **không** deploy — mã không đổi nên không cần. **Chưa ai làm theo tài liệu này**, kể cả trên máy chủ dự án. Bot đã gửi được tin thử thật tới `@Bao_tri_Saphia_Bot`, đó là mốc đã kiểm chứng duy nhất của hôm nay.
- **Việc cần làm tiếp theo:** Chủ dự án cài thử MacroDroid trên máy mình theo tài liệu, đặc biệt là phép thử chờ 15 phút. Chạy được rồi mới nhân ra cho thợ. Song song: ghép chat id đủ 15 thợ, xoá dãy số mượn tạm ở dòng thợ dùng để thử, rồi mới bật `TELEGRAM_BAT` lên `BAT` và cài trigger nhắc.

## [2026-09-10] Bot Telegram — ĐÃ `clasp push` 22 file lên Apps Script. Chưa deploy
- **Thay đổi:** Không sửa dòng mã nào. Đây là mục ghi **thay đổi trạng thái**: mã bot Telegram lần đầu lên hệ thống thật.
- **Trạng thái:** Đã push GitHub từ trước. **ĐÃ `clasp push`** lúc 08:37 ngày 10/09/2026, 22 file, có `ThongBao.gs`. **CHƯA deploy.** Chạy `kiemtra\kiem-tra.ps1` ngay trước khi push, sạch cả 5 lớp: KPI 2039/2039, nội dung tin 229/229. Dùng `--force`, an toàn vì `dongbo.ps1` cùng buổi sáng đã chứng minh repo mới hơn ở cả 5 file lệch và không ai sửa thẳng trên Editor. Chủ dự án xác nhận riêng cho lần push này.
- **Hệ thống đang chạy chưa đổi hành vi nào.** `clasp push` chỉ cập nhật mã HEAD; công nhân quét QR vẫn vào bản đã triển khai cũ cho tới khi deploy. Thứ đổi ngay là menu trong Sheet và mã mà trigger chạy. Công tắc `TELEGRAM_BAT` vẫn `TAT` nên phần Telegram là lệnh rỗng: hàm gửi chặn ngay cửa đầu tiên, chưa chạm tới mạng, thợ không nhận được gì.
- **Phép thử 4G đã xong**, chủ dự án báo ngày 10/09/2026. Đây là thứ chặn bước bật công tắc, nay đã gỡ. Bước 0 của mục 7 `TASK_THONG_BAO_TELEGRAM.md` vẫn ghi "chưa thử", **cần sửa**.
- **Bot đã tạo:** `@Bao_tri_Saphia_Bot`. Token cất ở Script Properties, khoá `TELEGRAM_BOT_TOKEN`, không nằm trong repo. Token đầu tiên `@BotFather` cấp đã bị dán vào khung chat nên coi như lộ; đã báo chủ dự án `/revoke`, quyết định cuối là của chủ dự án.
- **Chủ dự án chốt: giai đoạn này chỉ một mình test, thợ chưa nhận gì.** Làm được mà không cần bật công tắc, vì mục menu "Gửi tin thử" cố ý đi vòng qua `TELEGRAM_BAT` và cầu chì — đúng lý do đã ghi ở B5. Bẫy cần nhớ: chat id của chủ dự án đặt tạm vào dòng một thợ để thử, **thử xong phải xoá đi**, để quên ở đó mà sau này bật công tắc là thợ ấy nhận tin của người khác.
- **Việc cần làm tiếp theo:** Trong Sheet chạy menu "1. Cài đặt hệ thống", kiểm `Danh_Muc_Tho` có cột `Telegram_Chat_ID` ở đúng cuối và dữ liệu 13 thợ không xô lệch, kiểm `Cau_Hinh` có ba khoá mới với `TELEGRAM_BAT` = `TAT`. Rồi lấy Telegram ID và gửi tin thử. Sau đó mới tới ghép đủ 15 thợ, bật công tắc, cài trigger, và cuối cùng là deploy vào lần chủ nhật.

## [2026-09-10] `dongbo.ps1` — sửa cách đếm dòng ở vòng duyệt ngược
- **Thay đổi:** Bỏ `Measure-Object -Line`, đếm bằng `-split` y hệt nhánh so sánh phía trên.
- **Lý do:** Bản vá điểm mù chạy đúng ngay lần đầu trên máy công ty — `ThongBao.gs` hiện ra trong nhóm "có trong repo nhưng CHƯA có trên Apps Script" — nhưng báo **763 dòng** trong khi file có **826**. `Measure-Object -Line` bỏ qua dòng trống, mà file có đúng 63 dòng trống. Nguy ở chỗ nó không sai kiểu văng lỗi mà sai kiểu im lặng, và trong cùng một bản báo cáo có hai con số đếm theo hai kiểu khác nhau: nhóm "khác nội dung" đếm bằng `-split` (tính cả dòng trống), nhóm mới đếm bỏ dòng trống. Người đọc đem hai con số ra so là ra kết luận sai về việc bên nào nhiều hơn bao nhiêu.
- **Trạng thái:** Đã sửa, đã commit. **Không** `clasp push`, **không** deploy. Chưa chạy lại — phiên này không có PowerShell. Lần chạy tới phải thấy `ThongBao.gs` báo **826 dòng**, không phải 763.
- **Việc cần làm tiếp theo:** Chạy lại `dongbo.ps1` canh đúng con số 826. Rồi quay lại bot Telegram: phép thử 4G, `@BotFather`, `clasp push`.

## [2026-09-10] Vá điểm mù bước 4 của `dongbo.ps1` — nay so hai chiều
- **Thay đổi:** `dongbo.ps1` bước 4 thêm vòng duyệt ngược từ `bao-tri-v2/` sang bản tải về, liệt kê **file có trong repo mà Apps Script chưa có**, kèm số dòng. Thêm `$chiTrongRepo` và bảng tra `$tenTrenServer` dựng ngay trong vòng duyệt xuôi. Điều kiện "sạch" nay phải cả ba mảng cùng rỗng. Cập nhật khối chú thích đầu file. Không đụng mã `.gs` nào.
- **Lý do:** Vòng duyệt cũ chỉ đi từ server sang repo, nên file mới trong repo không bao giờ hiện ra. Sáng nay nó liệt kê 5 file lệch và kết luận "CO LECH", nhưng `ThongBao.gs` 826 dòng — file mới nhất, file sắp `clasp push` — **vắng mặt hoàn toàn**. Cái nguy không phải là báo thiếu, mà là báo thiếu đúng lúc người ta tin nhất: ngay trước khi quyết định push lên hệ thống 175 máy đang chạy.
- **Chỉ xét ba loại đuôi `clasp` thật sự đẩy lên:** `.gs`, `.html`, `appsscript.json`. Không xét `.md`, nếu không `bao-tri-v2/CLAUDE.md` sẽ bị báo đỏ mỗi lượt chạy, và một cảnh báo kêu sai mỗi ngày là cách nhanh nhất dạy người ta bỏ qua cảnh báo thật.
- **Một chỗ siết cú pháp:** điều kiện lọc viết `($_.Extension -in @('.gs','.html')) -or ($_.Name -eq 'appsscript.json')` có ngoặc đầy đủ. Bản không ngoặc dễ bị hiểu sai thứ tự ưu tiên giữa dấu phẩy, `-in` và `-or`.
- **Trạng thái:** Đã sửa, đã commit. **Không** `clasp push`, **không** deploy. **Chưa chạy thử** — phiên này chạy trên Linux, không có PowerShell nên không kiểm được cả cú pháp. Lần chạy tới trên máy công ty sẽ là phép thử thật, và ta biết trước kết quả đúng phải ra sao: `ThongBao.gs` 826 dòng phải xuất hiện trong nhóm "có trong repo nhưng chưa có trên Apps Script". Không thấy dòng đó tức là bản vá hỏng.
- **Ba commit hôm nay đã gộp vào `master`** bằng fast-forward, `master` ở `ef5d6ec`. Nhánh `claude/focused-knuth-1cq7aq` đã dọn.
- **Việc cần làm tiếp theo:** Chạy `dongbo.ps1` trên máy công ty, canh đúng dòng `ThongBao.gs`. Rồi quay lại bot Telegram: phép thử 4G, `@BotFather`, `clasp push`.

## [2026-09-10] `roi-may.ps1` chạy thật lần đầu, đạt. Nối vào `CLAUDE.md` và `README.md`
- **Thay đổi:** Khối đầu `CLAUDE.md` từ "BẮT ĐẦU MỖI PHIÊN" thành "HAI LỆNH CỦA MỘT PHIÊN", có cả `dongbo.ps1` lẫn `roi-may.ps1`; bảng mã thoát tách thành hai cột cho hai script. Mục "Bắt đầu mỗi phiên làm việc" của `README.md` viết lại tương ứng. Thêm vào cả hai chỗ dòng nhắc chạy trong PowerShell **thường**. Không sửa mã.
- **Chạy thật lần đầu đạt.** Máy công ty chạy hết năm bước và thoát `XONG`: bộ kiểm tra sạch (229 phép thử nội dung tin), không có gì để commit, `git pull --rebase` báo already up to date, không có commit nào chưa đẩy. Trước đó script chưa từng chạy và cũng chưa kiểm được cú pháp, vì phiên viết nó chạy trên Linux không có PowerShell.
- **Thêm dòng nhắc PowerShell thường vì đã trả giá hôm nay.** Cửa sổ quyền Administrator dùng hồ sơ người dùng khác nên không có `git` lẫn `clasp.cmd` trong PATH, lỗi hiện ra là `The term 'git' is not recognized` — đọc như thể Git chưa cài, mất một lúc mới ra. `roi-may.ps1` cũng bắt đúng câu đó ở bước 5 và nhắc thẳng nguyên nhân.
- **Trạng thái:** Đã sửa tài liệu, đã commit. **Không** `clasp push`, **không** deploy. Hai commit trước (`581ddbc` sửa trạng thái B1–B8, `dfc0354` thêm `roi-may.ps1`) đã lên GitHub trên nhánh `claude/focused-knuth-1cq7aq`, kiểm lại bằng API thấy đủ. **`master` chưa có hai commit đó**, còn nằm ở `0cbe727`.
- **Một điểm mù của `dongbo.ps1` phát hiện hôm nay, CHƯA sửa.** Bước 4 chỉ duyệt file có trên Apps Script, nên **file có trong repo mà chưa có trên server thì không bị báo gì cả**. Sáng nay nó liệt kê 5 file lệch nhưng `ThongBao.gs` 826 dòng vắng mặt hoàn toàn, dù đó mới là file mới nhất. Lần này vô hại vì ta biết trước, nhưng nên sửa trước bước `clasp push` của bot Telegram — đúng lúc cần nhìn rõ hai bên lệch chỗ nào thì lại là lúc điểm mù ấy nguy hiểm nhất.
- **Máy công ty hôm nay tụt rất xa.** `dongbo.ps1` fast-forward từ `1972d7d` lên `0cbe727`, 15 file, +2670 dòng: toàn bộ bot Telegram B1–B8 hôm qua chưa từng có trên máy này. Đối chiếu số dòng lệch với Apps Script khớp đúng từng con số của phần Telegram (7, 25, 9, 13, 192), nên **không ai sửa thẳng trên Editor** — bản trên server vẫn đứng nguyên ở trạng thái trước Telegram, đúng như thiết kế.
- **Việc cần làm tiếp theo:** Gộp nhánh `claude/focused-knuth-1cq7aq` vào `master`. Sửa điểm mù bước 4 của `dongbo.ps1`. Phần bot Telegram không đổi: phép thử 4G, rồi `@BotFather`, rồi `clasp push`.

## [2026-09-10] Thêm `roi-may.ps1` — nửa còn thiếu của quy trình hai máy
- **Thay đổi:** Thêm `roi-may.ps1` ở gốc repository, 190 dòng. Chạy trước khi rời máy, làm năm bước, hỏng ở đâu dừng ở đó: kiểm thư mục có phải repo và có đang trên nhánh không, chạy `kiemtra\kiem-tra.ps1`, hỏi xác nhận rồi commit, `git pull --rebase`, `git push`. Kết luận cuối so `HEAD` với `origin/<nhánh>` và soi lại `git status` để chắc là đã hết lệch. Có `-BoQuaKiemTra` giống `-BoQuaClasp` của `dongbo.ps1`. **Không** đụng `clasp push`, **không** deploy, không tự sửa file nào trong repo.
- **Lý do:** `dongbo.ps1` chỉ lo lúc ngồi xuống, lúc đứng dậy thì không có gì cả nên phải tự nhớ commit rồi push. Đó đúng là chỗ đã hỏng ba lần: repo tụt ~1000 dòng, 7 file kẹt trên laptop hôm 03/09, và commit `NOTES.md` kẹt hôm nay. Từ giờ quy trình gọn còn hai lệnh thuộc lòng được: `dongbo.ps1` lúc ngồi xuống, `roi-may.ps1` lúc đứng dậy.
- **Hai điểm chủ dự án chốt.** (1) **Hỏi xác nhận mỗi lần commit**, không tự đoán theo loại file. Đây là lần cuối còn người ngồi đấy để nhìn danh sách file; đoán sai thì máy kia kéo về đúng cái sai mà không ai còn ngồi trước màn hình. Mô tả commit rỗng cũng bị chặn. (2) **Xung đột rebase thì dừng hẳn**, để nguyên hiện trường cho người tự gỡ, và in sẵn ba lệnh `git status`, `git rebase --continue`, `git rebase --abort`. Cố ý **không** tự `--abort`: tự bỏ là xoá mất phần đã gỡ được và giấu luôn chuyện có xung đột.
- **Dùng `--rebase` chứ không `merge`.** Hai máy cùng một người nên xếp nối tiếp đọc log dễ hơn, và không sinh một merge commit rác mỗi lần đổi máy. Khác với luật cấm rebase nhánh người khác, ở đây chỉ có một người.
- **Trạng thái:** Đã viết, **đã commit, CHƯA push GitHub** — phiên này chạy trên Linux và `git push` bị chặn quyền. **Không** `clasp push`, **không** deploy. **Chưa chạy thử được một lần nào**: phiên không có PowerShell nên không kiểm được cả cú pháp. Lần chạy đầu tiên phải là trên máy công ty và nên chạy khi cây làm việc sạch, để nếu script sai thì nó chỉ dừng chứ không commit nhầm. Bộ kiểm tra bằng node vẫn sạch: token 44 file, chồng tham số sạch, KPI 2039/2039, nội dung tin 229/229.
- **Chưa nối vào tài liệu.** Khối "BẮT ĐẦU MỖI PHIÊN" ở đầu `CLAUDE.md` và `README.md` vẫn chỉ nói tới `dongbo.ps1`. Chờ script chạy thật một lần rồi mới sửa hai chỗ đó, vì viết vào tài liệu một lệnh chưa ai chạy bao giờ là mời người khác tin nhầm.
- **Việc cần làm tiếp theo:** Chạy thử `roi-may.ps1` trên máy công ty với cây sạch, xem nó đi hết năm bước và thoát 0. Chạy được thì thêm vào `CLAUDE.md` và `README.md`. Phần bot Telegram không đổi: phép thử 4G, rồi `@BotFather`, rồi `clasp push`.

## [2026-09-10] Sửa dòng trạng thái B1–B8 trong `NOTES.md` cho khớp thực tế
- **Thay đổi:** 8 mục B1–B8 đều ghi "**đã commit, CHƯA push GitHub** (lệnh push chủ dự án tự chạy)", sửa thành "**đã commit, đã push GitHub**". Ở mục B8 sửa thêm lý do: "Mục 20 và 20b chưa chạy trong Sheet vì chưa push" thành "vì chưa `clasp push`". Không sửa một dòng mã nào.
- **Lý do:** Các mục đó viết lúc commit còn nằm trên laptop cá nhân. Sau đó đã push, `origin/master` hiện ở đúng `0cbe727` của B8, nên dòng trạng thái thành sai. Dòng đầu `TASK_THONG_BAO_TELEGRAM.md` ghi đúng là đã push, tức là hai tài liệu nói ngược nhau — đọc `NOTES.md` trước sẽ tưởng GitHub còn thiếu B1–B8 và đi push lại hoặc đi tìm commit đã có. Riêng lý do ở B8 sai theo kiểu khác: mục 20 và 20b thật sự chưa chạy trong Sheet, nhưng vì mã chưa lên Apps Script, không phải vì chưa lên GitHub; để nguyên thì lần sau push GitHub xong lại tưởng đã chạy được test trong Sheet.
- **Trạng thái:** Đã sửa tài liệu. **Không** `clasp push`, **không** deploy; mã nguồn không đổi. Chạy lại bộ kiểm tra bằng node trên phiên Linux: chốt token 44 file sạch, chồng tham số sạch, KPI 2039/2039, nội dung tin 229/229, cú pháp các file `.gs` hợp lệ. **Chưa chạy được `dongbo.ps1`** nên chưa đối chiếu với Apps Script Editor — phiên này không có PowerShell lẫn `clasp`; lần sau ngồi máy công ty phải chạy để chốt nốt lớp đó.
- **Con số 48 file ở các mục cũ để nguyên, không sửa.** Chốt token bây giờ quét 44 file. Đó là số đo tại thời điểm khác, không phải lỗi, và `NOTES.md` là nhật ký chứ không phải bảng trạng thái hiện tại — sửa số đo cũ theo số đo mới là xoá mất vết.
- **Việc cần làm tiếp theo:** Không đổi so với B8. Phép thử 4G của thợ chưa làm, nó chặn bước bật `TELEGRAM_BAT`. Rồi `@BotFather` lấy token dán vào Script Properties, rồi mới tới `clasp push` (cần xác nhận riêng từng lần).

## [2026-09-09] Bước B8 bot Telegram — kiểm thử trong Sheet và tài liệu. XONG B1–B8
- **Thay đổi:** `Test.gs` thêm mục 20 và 20b, 41 ca. `bao-tri-v2/CLAUDE.md` thêm hẳn mục **6b Thông báo Telegram**, cập nhật mục 2 (21 lên 22 file), mục 3 (`Danh_Muc_Tho` 10 cột và lý do phải để định dạng text), mục 12 (mục menu mới, 4 trigger), mục 13 (hai bẫy mới), mục 14 (~250 test), mục 15 (khối trạng thái bot Telegram và 7 bước đưa vào chạy). Cập nhật dòng trạng thái đầu `TASK_THONG_BAO_TELEGRAM.md`.
- **Nhóm ca 20b là phần duy nhất không chép sang bộ tại máy được**, vì `lienLacTho_` đọc `Danh_Muc_Tho` và `thongBaoDaNhan_` gọi `getOnDutyContacts_`. Cách làm: gán đè ba hàm đó bằng hàm giả trong `try`, **trả lại trong `finally`**, rồi có ba ca canh đã trả lại đúng chưa. Quên trả lại là mọi test chạy sau đó dùng nhầm hàm giả, nguy hiểm hơn hẳn một test đỏ vì nó xanh mà sai — đã ghi thành bẫy mới ở mục 13.
- **Canh được rào 5.1 tới đâu:** tài liệu bảo canh `reportIncident` vẫn trả `ok: true` khi hàm gửi ném lỗi, nhưng `Test.gs` không đọc/ghi sheet nào nên không gọi được `reportIncident`. Canh hai lớp bọc thay thế: hàm gửi ném lỗi thì `thongBaoSuCoMoi_` vẫn trả 0 và không ném; hàm đọc sheet ném lỗi cũng vậy. Lớp thứ ba là `try/catch` tại chỗ gọi trong `CongNhan.gs`, đọc mắt thấy được, không có test.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. Bộ kiểm tra tại máy sạch: chốt token 48 file, KPI 2039/2039, nội dung tin 229/229. Mục 20 và 20b **chưa chạy trong Sheet** vì chưa `clasp push`; đã dựng khung tạm chạy thử tại máy bằng node, 41/41 đạt, và kiểm chứng ngược bằng bản cố tình làm hỏng (bỏ phép loại người vừa bấm nhận) thì báo đúng 5 ca đỏ.
- **B1 tới B8 xong. Mã cho bot Telegram đã đủ.** Việc còn lại là mục 7 của tài liệu, phần đưa vào chạy thật, và nó cần chủ dự án làm bằng tay: thử Telegram trên 4G của thợ, nhắn `@BotFather` lấy token dán vào Script Properties, rồi mới tới `clasp push` (cần xác nhận riêng từng lần).
- **Việc cần làm tiếp theo:** Phép thử 4G chưa làm — nhờ một thợ tắt wifi rồi mở Telegram. Việc đó không chặn `clasp push` nhưng chặn bước bật công tắc, vì thợ ca đêm rớt wifi sang 4G đúng lúc cần nhận tin nhất.

## [2026-09-09] Bước B7 bot Telegram — trigger nhắc 5 phút. Mã đã đủ, còn kiểm thử và tài liệu
- **Thay đổi:** `ThongBao.gs` thêm hai hàm thuần `demLanDaNhac_`, `chonPhieuCanNhac_` và mục 8 với `nhacPhieuChoNhan()`. `BaoCao.gs` nối trigger 5 phút vào `caiDatTrigger` (3 trigger thành 4). Thêm bốn hằng vào `TELEGRAM`. Thêm 36 phép thử, tổng 229. Sửa rào 5.10 của `TASK_THONG_BAO_TELEGRAM.md`.
- **Không dùng `LockService` cho trigger, khác chữ của rào 5.10.** Khoá script là khoá dùng chung: giữ nó suốt lượt trigger là chặn luôn công nhân báo sự cố trong lúc bot gọi mạng, đúng thứ rào 5.2 cấm; và khi Telegram treo thì khoá bị giữ tới hết giờ chờ, tức là gộp cả hai cách hỏng mà 5.2 với 5.12 sinh ra để tránh. Dùng cờ trong `CacheService` với hạn 240 giây, chống chạy chồng đúng như rào muốn mà không đụng tới ai. Giữ nguyên ý của rào, đổi cách làm.
- **Ba quyết định khác đáng ghi.** (1) **Nhảy thẳng lên lần nhắc thứ 2** khi phiếu đã quá cả hai ngưỡng lúc mới xét lần đầu, vì trigger chạy trễ hoặc script bị tắt một lúc; nhắc "lần 1" cho phiếu đã treo 3 tiếng là nói sai sự thật. (2) **Trần 1 ngày.** Lý do chính không phải là phiếu cũ vô nghĩa, mà là rào chặn lúc gõ `BAT` lần đầu — không có nó thì công tắc vừa bật là bot bắn một loạt tin về những phiếu cũ còn treo từ trước, đúng cách làm người ta tắt bot ngay ngày đầu. (3) **Ghi nhật ký SAU khi gửi và chỉ ghi khi gửi được ít nhất một tin**: gửi hỏng thì không để lại vết, lượt sau thử lại. Đổi lại nếu script chết đúng khe giữa hai việc đó thì phiếu bị nhắc lặp một lần — hiếm, và một tin thừa nhẹ hơn một tin mất.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. Bộ kiểm tra sạch: chốt token 48 file, KPI 2039/2039, nội dung tin 229/229. Trigger chỉ đọc `Su_Co`, chỉ ghi `Nhat_Ky_Su_Co` (rào 5.9). Cài sẵn trigger từ trước lúc bật công tắc là vô hại: `TELEGRAM_BAT` tắt thì mỗi lượt chỉ đọc một ô cấu hình rồi thoát.
- **Mã cho bot Telegram đến đây là đủ.** Luồng đang chạy gọi vào `ThongBao.gs` qua đúng ba cửa: `thongBaoSuCoMoi_` từ `reportIncident`, `thongBaoDaNhan_` từ `acceptIncident`, và `nhacPhieuChoNhan` từ trigger.
- **Việc cần làm tiếp theo:** Bước B8 — thêm mục kiểm thử vào `Test.gs` với hàm gửi bị thay bằng hàm giả, canh đúng số tin và đúng người nhận, và canh `reportIncident` vẫn trả `ok: true` khi hàm gửi ném lỗi (rào 5.1). Nhớ bẫy: ca kiểm thử chép làm hai bản ở `kiemtra/thongbao.js` và `Test.gs`, sửa bên nào phải sửa cả hai. Rồi cập nhật `bao-tri-v2/CLAUDE.md`.

## [2026-09-09] Bước B6 bot Telegram — móc vào luồng đang chạy, hai chỗ chứ không phải ba
- **Thay đổi:** `ThongBao.gs` thêm `locNguoiNhan_` (hàm thuần), `lienLacTho_` thay cho `chatIdTheoMaTho_` (nay là vỏ bọc mỏng), và mục 7 với hai hàm ghép `thongBaoSuCoMoi_`, `thongBaoDaNhan_`. `CongNhan.gs` móc vào `reportIncident`. `LuongTho.gs` móc vào `acceptIncident`. Thêm 18 phép thử, tổng 193. Sửa mục 4.2 của `TASK_THONG_BAO_TELEGRAM.md`.
- **Bỏ chỗ móc thứ ba, `reportMachineStop`.** Tài liệu duyệt hôm nay liệt kê ba chỗ, nhưng vào đọc mã mới thấy không hợp: phiếu `DM-` không có thợ nào, không đi qua màn hình nhận việc, nên dòng "Bấm để nhận việc" sai hẳn với loại phiếu đó, và hàm cũng không hề dựng danh bạ. Năm trong sáu lý do dừng máy đang khai không ai gọi thợ; phiếu chờ phụ tùng thì do chính thợ tự tạo nên bot sẽ nhắn lại cho người vừa bấm. Cái hại nặng và khó thấy hơn là **thợ chai với thông báo** — tin không kèm việc gì để làm sẽ dạy người ta lướt qua, đến lúc tin sự cố thật tới thì phản xạ đã thành bỏ qua. Điểm mất duy nhất là ca mất điện, lúc đó tổ điện có việc thật; để mở phương án hẹp là chỉ gửi với những lý do khai trong một khoá cấu hình mới. Chủ dự án chọn bỏ sau khi so hai hướng.
- **Một chỗ tài liệu chỉ sai vị trí:** mục 4.2 bảo đặt lời gọi trong `acceptIncident` "ngay trước `return`", nhưng hàm đó **giữ khoá tới cuối và nhả trong `finally`**, nên chỗ ấy vẫn nằm TRONG khoá và vi phạm thẳng rào 5.2. Đã thêm cặp `daMoKhoa` / `moKhoa_()` đúng lối `reportIncident` đang dùng, nhả khoá rồi mới gửi. Đây là thay đổi thật vào một hàm đang chạy, nhưng là lối đã có sẵn trong chính mã nguồn này.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. Bộ kiểm tra sạch: chốt token 48 file, KPI 2039/2039, nội dung tin 193/193. Công tắc `TELEGRAM_BAT` mặc định `TAT` nên kể cả sau khi push, phần Telegram vẫn là lệnh rỗng cho tới khi có người gõ `BAT`. Danh sách người nhận tin "đã có người nhận" dựng lại bằng mốc **giờ báo hỏng**, không phải giờ hiện tại, để ra đúng nhóm đã nhận tin lần đầu kể cả khi ca đã đổi giữa chừng.
- **Cái giá đã biết và chấp nhận:** công nhân bấm gửi phiếu chờ thêm một lượt đọc `Danh_Muc_Tho` (~150 ms) và một cuộc gọi mạng trước khi thấy màn hình danh bạ. Cầu chì cắt sau lần hỏng đầu nên khi Telegram chậm thì chỉ một người chịu mỗi mười phút.
- **Việc cần làm tiếp theo:** Bước B7 — `nhacPhieuChoNhan()` chạy bằng trigger 5 phút, nối vào `caiDatTrigger`. Chỉ đọc `Su_Co`, chỉ ghi `Nhat_Ky_Su_Co` (rào 5.9), `tryLock` ngắn rồi bỏ lượt nếu không lấy được khoá (rào 5.10), mỗi phiếu tối đa 2 lần nhắc đời đời (rào 5.11).

## [2026-09-09] Bước B5 bot Telegram — hai mục menu và chốt chặn rò rỉ token
- **Thay đổi:** `ThongBao.gs` thêm `gomChatIdTuUpdates_` (hàm thuần), `goiApiTelegram_`, `menuLayTelegramId`, `menuGuiThu`. `Code.gs` thêm submenu **📨 Thông báo Telegram** với hai mục đó. Thêm `kiemtra/token.js` và nối làm **chốt 0** của `kiem-tra.ps1`, chạy trước mọi lớp. Thêm 18 phép thử, tổng 175. Ghi chốt 0 vào mục 14 của `bao-tri-v2/CLAUDE.md`.
- **Lý do:** Ba quyết định. (1) **Hai mục menu cố ý KHÔNG đi qua công tắc `TELEGRAM_BAT` và không đi qua cầu chì.** Mục 7 của tài liệu xếp thứ tự là ghép chat id và gửi thử TRƯỚC, bật công tắc SAU; bắt chúng chờ công tắc là không cách nào gửi thử được, mà bật công tắc trước khi thử là đúng thứ tài liệu bảo đừng làm. (2) **Hai mục menu cố ý KÊU TO khi hỏng**, ngược hẳn `guiTelegram_` vốn nuốt mọi lỗi: người bấm menu đang ngồi tìm nguyên nhân nên cần thấy mã lỗi và nguyên văn câu trả lời của Telegram, còn công nhân báo sự cố thì không cần biết gì về Telegram. Hai lỗi hay gặp nhất đọc thẳng ra được từ đó: 403 là thợ chưa bấm START với bot, 400 "chat not found" là chat id ghép nhầm dòng. (3) Chốt token **không in ra token tìm được** — in ra là chép nó thêm một lần nữa vào nhật ký chạy lệnh; chỉ báo đường dẫn với số dòng, kèm nhắc phải `/revoke` chứ không phải chỉ xoá dòng đó đi.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. Bộ kiểm tra sạch: chốt 0 quét 48 file không thấy token, KPI 2039/2039, nội dung tin 175/175. Đã kiểm chứng chốt 0 bắt được lỗi thật bằng một file có token giả trong thư mục tạm: báo đúng đường dẫn và số dòng, mã thoát 1. `kiemtra/` nằm ngoài thư mục clasp nên `token.js` không bị đẩy lên Apps Script. Ngoài hai mục menu người tự bấm, **vẫn chưa chỗ nào trong luồng đang chạy gọi tới `ThongBao.gs`**.
- **Việc cần làm tiếp theo:** Bước B6 — móc vào ba chỗ: `reportIncident` và `reportMachineStop` trong `CongNhan.gs`, `acceptIncident` trong `LuongTho.gs`. Mỗi chỗ chỉ thêm một lời gọi, bọc try/catch tại chỗ, và phải đặt **sau** `moKhoa_()` theo rào 5.2 — đặt nhầm vào trong khoá thì mọi người báo sự cố phải xếp hàng chờ một cuộc gọi mạng. Đây là bước đụng thẳng vào luồng đang chạy, trình bày phương án trước.

## [2026-09-09] Bước B4 bot Telegram — cột Telegram_Chat_ID và ba khoá cấu hình
- **Thay đổi:** `Code.gs` — thêm `Telegram_Chat_ID` vào **cuối** `HEADER_THO` (9 lên 10 cột); thêm ba khoá vào `CAU_HINH_MAC_DINH`: `TELEGRAM_BAT` mặc định `TAT`, `NHAC_LAN_1_PHUT` = 10, `NHAC_LAN_2_PHUT` = 20; `setupSystem` đặt định dạng text và bề rộng cho cột mới. Thêm 4 phép thử vào `kiemtra/thongbao.js`, tổng 157. Sửa rào 5.3 của `TASK_THONG_BAO_TELEGRAM.md`. **Đây là lần đầu đụng vào file đang chạy**, chủ dự án duyệt trước khi sửa.
- **Lý do:** Điểm kỹ thuật quan trọng nhất là **định dạng text cho cột chat id**, đúng cách cột `So_Dien_Thoai` đang làm. Chat id dài 10–13 chữ số; để ô ở dạng số thì Sheets hiển thị thành dạng mũ khi cột hẹp, mà `chuanHoaChatId_` viết ở B3 cố ý chặn dạng mũ vì gửi tới một id đã bị làm tròn là nhắn nhầm người khác. Quên dòng đó thì thợ ấy bị bỏ qua im lặng, không ai biết vì sao. Phép thử mới canh cột nằm đúng **cuối** header: chèn vào giữa là xô lệch dữ liệu đã ghi của 13 thợ, và hai hàm đọc-ghi trọn khối theo chỉ số cột sẽ ghi nhầm ô.
- **Trạng thái:** Đã sửa, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. `kiemtra\kiem-tra.ps1` sạch cả 5 lớp: KPI 2039/2039, nội dung tin 157/157. **Sheet ngoài nhà máy vẫn y nguyên 9 cột** — `setupSystem` chỉ chạy được sau `clasp push`, đó là bước 2 của mục 7. `chatIdTheoMaTho_` viết ở B3 đã lường trước nên chạy đúng ở cả hai trạng thái.
- **Đính chính rào 5.3:** tài liệu ghi "cột mới chỉ ĐỌC, không ghi" là **sai**. Có hai hàm đọc rồi ghi lại trọn khối `HEADER_THO.length` cột: `refreshPersonalLinks` (menu 4) và `chuanBiDanhMuc` (menu 3). Sau B4 chúng ghi 10 cột thay vì 9. Vẫn an toàn, nhưng vì chúng ghi lại chính giá trị vừa đọc trong cùng một lượt, chứ không phải vì "không ghi". Kết luận của rào không đổi, lý do thì khác.
- **Việc cần làm tiếp theo:** Bước B5 — hai mục menu (`menuLayTelegramId` gọi `getUpdates`, `menuGuiThu`), và phép kiểm tra token trong `kiemtra` theo rào 5.13: quét file `.gs` tìm chuỗi dạng token bot Telegram, thấy là báo đỏ và chặn push.

## [2026-09-09] Bước B3 bot Telegram — phần gửi thật, công tắc, cầu chì, map chat id
- **Thay đổi:** Thêm vào cuối `bao-tri-v2/ThongBao.gs`: ba hàm thuần `chuanHoaChatId_`, `catTin_`, `nenBatCauChi_`; hằng `TELEGRAM`; `tokenTelegram_`, `telegramBat_`, `cauChiDangNgat_`, `batCauChi_`, `guiTelegram_`, `chatIdTheoMaTho_`. Thêm 44 phép thử vào `kiemtra/thongbao.js`, tổng thành 153. **Vẫn chưa file nào gọi tới `ThongBao.gs`** — không sửa một dòng nào của các file đang chạy.
- **Lý do:** Bốn quyết định đáng ghi. (1) `guiTelegram_` có **bốn cửa chặn trước khi chạm mạng**, xếp rẻ tiền nhất trước: danh sách rỗng → công tắc `TELEGRAM_BAT` tắt → chưa có token → cầu chì đang ngắt. Công tắc mặc định TẮT, và mọi đường hỏng đều dẫn về tắt: thiếu khoá, gõ sai chính tả, đọc sheet ném lỗi. (2) **Cầu chì phân biệt hai kiểu hỏng.** 429 và 5xx là hỏng phía hệ thống, gọi tiếp cũng hỏng mà mỗi lần gọi là một người phải chờ, nên bật cầu chì 10 phút. Còn 400 và 403 là hỏng ở một chat id cụ thể — ghép nhầm id, hoặc thợ chưa bấm Start với bot — bật cầu chì vì chuyện đó là cắt tin của cả 14 người còn lại. Phép quyết định này nằm trong hàm thuần `nenBatCauChi_` nên kiểm thử được tại máy. (3) `muteHttpExceptions: true` trên từng yêu cầu: thiếu cờ này thì một thợ chưa bấm Start làm hỏng luôn tin của cả ca. (4) `chatIdTheoMaTho_` **tự nối cột `Telegram_Chat_ID` khi `HEADER_THO` chưa có nó**, nên chạy đúng cả trước lẫn sau B4; sheet mặc định rộng 26 cột nên đọc 10 cột vẫn trong vùng, không văng lỗi tràn cột kể cả khi chưa chạy `setupSystem`.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy. `kiemtra\kiem-tra.ps1` sạch cả 5 lớp: KPI 2039/2039, nội dung tin 153/153. Ranh giới hàm thuần nay ghi rõ ở đầu file: mục 1, 2, 3 thuần; từ mục 4 trở xuống mới được đụng `PropertiesService`, `CacheService`, `UrlFetchApp`, `SpreadsheetApp`. Lớp 5 canh ranh giới đó cho 12 hàm.
- **Việc cần làm tiếp theo:** Bước B4 — thêm `Telegram_Chat_ID` vào **cuối** `HEADER_THO`, và ba khoá vào `CAU_HINH_MAC_DINH`: `TELEGRAM_BAT` mặc định `TAT`, `NHAC_LAN_1_PHUT`, `NHAC_LAN_2_PHUT`. Đây là bước đầu tiên đụng vào file đang chạy (`Code.gs`), nên trình bày phương án trước.

## [2026-09-09] Bước B2 bot Telegram — lớp 5 kiểm thử nội dung tin, chạy tại máy
- **Thay đổi:** Thêm `kiemtra/thongbao.js` — 109 phép thử nội dung tin, nạp `Code.gs` + `ThongBao.gs` bằng `vm.runInThisContext`. Nối thành lớp 5 của `kiemtra/kiem-tra.ps1`. Cập nhật mục 14 của `bao-tri-v2/CLAUDE.md` từ 5 lớp lên 6 lớp, và sửa dòng ghi chú ở mục 12 vốn còn ghi "3 lớp kiểm tĩnh" từ trước. **Không sửa một dòng mã `.gs` nào.**
- **Lý do:** Ba hàm soạn tin là hàm thuần nên kiểm thử được ngay tại máy, không phải mở Sheet. Ngoài các ca canh nội dung, lớp này có một ca đáng nói: **canh ranh giới hàm thuần**. Bước B3 sẽ thêm phần gửi thật vào chính `ThongBao.gs`, mà phần đó có `UrlFetchApp` và `CacheService`. Nếu ai kéo nhầm một lời gọi như vậy vào nhóm hàm soạn tin thì mất luôn khả năng kiểm thử tại máy, và node sẽ ném `ReferenceError` với vết gọi khó đọc che mất nguyên nhân. Nên ca này soi mã nguồn TỪNG HÀM bằng `Function.prototype.toString` (không grep cả file — grep cả file thì tới B3 là đỏ oan), chạy TRƯỚC mọi ca khác, và in rồi dừng ngay tại chỗ.
- **Trạng thái:** Đã viết, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy — `ThongBao.gs` vẫn chưa được gọi từ đâu cả. `kiemtra\kiem-tra.ps1` sạch cả 5 lớp: KPI 2039/2039, nội dung tin 109/109. Đã kiểm chứng bộ test bắt được lỗi thật, không phải chỉ xanh suông: chép `bao-tri-v2` ra thư mục tạm rồi cố tình làm hỏng hai kiểu — nhét `Utilities` vào `tenMayDayDu_` thì lớp 5 báo đúng tên hàm và đúng thứ bị cấm rồi dừng; đổi lời văn và bỏ điều kiện `lanHai` thì báo 4 ca đỏ kèm chuỗi mong đợi và chuỗi thực tế.
- **Việc cần làm tiếp theo:** Bước B3 — viết phần gửi thật (`tokenTelegram_`, `telegramBat_`, `cauChiDangNgat_`, `guiTelegram_`, `chatIdTheoMaTho_`), đặt ở CUỐI `ThongBao.gs`, giữ nguyên tính thuần của nhóm hàm soạn tin. Ca kiểm thử ở `Test.gs` là bước B8, lúc đó các ca trong `kiemtra/thongbao.js` thành bản sao — sửa bên nào phải sửa cả hai.

## [2026-09-09] Bước B1 bot Telegram — ba hàm soạn tin thuần trong `ThongBao.gs`
- **Thay đổi:** Thêm file mới `bao-tri-v2/ThongBao.gs`, hiện **chỉ có phần soạn tin**: `soanTinSuCoMoi_(v, link)`, `soanTinDaNhan_(v)`, `soanTinNhac_(v, lan, khi, link, sdtKhanCap)`, kèm năm hàm phụ `gioVN_`, `chuoi_`, `phutGiua_`, `ghepDong_`, `ghepKhoi_`, `tenMayDayDu_`. Nội dung bám mục 4.3 của `TASK_THONG_BAO_TELEGRAM.md`. **Không sửa một dòng nào của các file đang chạy.**
- **Lý do:** Mục 4.1 tách phần soạn tin khỏi phần gửi để kiểm thử được tại máy bằng node, không cần mở Sheet. Ba quyết định đáng ghi: (1) không dùng cả `Utilities`, tự có `gioVN_` cộng cứng 7 giờ vào mốc UTC, vì `Utilities` chỉ tồn tại trong Apps Script mà bước B2 sẽ nạp file này vào node; (2) tin là **văn bản trơn, cố ý không dùng `parse_mode`** — mô tả sự cố do công nhân gõ tay có thể chứa `<`, `*`, `_`, bật Markdown hay HTML lên là Telegram trả lỗi 400 hoặc hiển thị sai, và trơn thì không có đường cho nội dung người nhập chen mã định dạng vào tin; (3) tham số thứ hai của `soanTinSuCoMoi_` đổi từ `may` trong tài liệu thành `link`, vì tên máy và bộ phận đã nằm sẵn ở `Ten_May`/`Bo_Phan` của `v`, còn thứ đổi theo từng người nhận là `Link_Ca_Nhan`. Chủ dự án duyệt đổi ngày 09/09/2026.
- **Trạng thái:** Đã viết mã, **đã commit, đã push GitHub**. **Không** `clasp push`, **không** deploy — file này chưa được gọi từ đâu cả, hệ thống đang chạy ở nhà máy không đổi gì. `kiemtra\kiem-tra.ps1` sạch cả 4 lớp, KPI 2039/2039. Đã chạy tay ba hàm bằng node với phiếu `SC-`, phiếu `DM-` không có nhóm lỗi, và phiếu rỗng hết: ô trống tự rụng dòng, không hàm nào văng lỗi.
- **Việc cần làm tiếp theo:** Bước B2 — thêm `kiemtra/thongbao.js` làm lớp 5, nạp `Code.gs` + `ThongBao.gs` bằng `vm.runInThisContext` (**không** `createContext`, xem bẫy ở mục 14 của `bao-tri-v2/CLAUDE.md`), rồi nối vào `kiem-tra.ps1`. Sau đó mới tới B3 viết phần gửi thật.

## [2026-09-09] Thử Telegram trên wifi nhà máy — vào được, và sửa một chỗ sai trong kế hoạch
- **Thay đổi:** Sửa mục 3 và mục 5.12 của `TASK_THONG_BAO_TELEGRAM.md`, cập nhật bước 0 ở mục 7. Bản đầu ghi rằng nếu mạng nhà máy chặn Telegram thì mỗi lần công nhân báo sự cố phải chờ lệnh gọi mạng chết ngắc. **Sai.** Apps Script chạy trên máy chủ Google nên lời gọi tới Telegram đi thẳng từ Google sang Telegram, không qua mạng nhà máy. Mạng nhà máy chỉ quyết định thợ có nhận và đọc được tin hay không. Cầu chì ở 5.12 vẫn giữ nhưng đổi lý do: phòng Telegram lỗi hoặc chặn tốc độ, và Google gọi ra ngoài chậm.
- **Lý do:** Chẩn đoán sai chỗ thì rào an toàn đặt sai chỗ, và một rào chặn `clasp push` không có thật sẽ giữ việc lại vô cớ.
- **Trạng thái:** Mới là tài liệu, đã push GitHub. **Không** `clasp push`, **không** deploy. Đã thử Telegram trên wifi nhà máy ngày 09/09/2026, vào được bình thường. **Chưa thử 4G của thợ** — thợ ca đêm hoặc đi tới khu máy sóng yếu thì rớt sang 4G, đúng lúc cần nhận tin nhất.
- **Việc cần làm tiếp theo:** Nhờ một thợ tắt wifi rồi mở Telegram để chốt phép thử 4G. Song song đó bắt đầu được bước B1 ở mục 6 của tài liệu, vì việc viết mã không phụ thuộc kết quả đó.

## [2026-09-09] Chốt phương án bot Telegram nhắc thợ, chưa viết mã
- **Thay đổi:** Thêm `TASK_THONG_BAO_TELEGRAM.md` — kế hoạch đầy đủ cho bot Telegram nhắc thợ khi có sự cố: thiết kế file `ThongBao.gs`, ba chỗ móc vào `reportIncident` / `reportMachineStop` / `acceptIncident`, ba lớp nhắc, 13 rào an toàn, 9 bước thi công, 7 bước đưa vào chạy thật, và prompt mở chat mới. Thêm mục việc đang chờ vào `PROMPT_KHUNG_CHAT_MOI.md`. **Chưa sửa một dòng mã nào của hệ thống.**
- **Lý do:** Công nhân quét QR báo sự cố xong thì quên gọi thợ rất nhiều, phiếu nằm `CHO_NHAN` không ai biết. Hệ thống đã biết chính xác ai đang trực, chỉ thiếu đường đẩy tin tới điện thoại thợ.
- **Trạng thái:** Mới là tài liệu, **đã push GitHub**. **Không** `clasp push`, **không** deploy. Chủ dự án đã duyệt 4 thứ: thêm cột `Telegram_Chat_ID` vào cuối `Danh_Muc_Tho`, ba khoá `Cau_Hinh`, token cất ở Script Properties, hai mục menu mới. Đã chốt gửi cho cả hai thợ đang trực thay vì gửi riêng người chính kèm nút chuyển việc — lý do ghi ở mục 2 và mục 9 của tài liệu. Kiểm chứng được một điểm quan trọng: `Danh_Muc_Tho` đi từ 9 lên 10 cột thì `docSheet_` không văng lỗi tràn cột kể cả khi chưa chạy `setupSystem`, vì sheet mặc định rộng 26 cột và ta chỉ đọc chứ không ghi. Khác hẳn đợt `Su_Co` 28 lên 33.
- **Việc cần làm tiếp theo:** Thử Telegram trên wifi nhà máy và 4G của thợ — chưa có kết quả này thì không `clasp push`, vì nếu bị chặn thì mỗi lần báo sự cố công nhân phải chờ lệnh gọi mạng chết ngắc. Vào được thì bắt đầu bước B1 trong mục 6 của tài liệu.

## [2026-09-09] Ghép phần việc còn kẹt trên laptop cá nhân vào repository
- **Thay đổi:** Laptop cá nhân còn 7 file sửa chưa commit từ 03/09. Phần mã `.gs` thì GitHub đã có đủ vì máy công ty kéo về từ Apps Script Editor sáng nay, nên bỏ bản laptop và lấy bản GitHub. Phần chưa từng lên GitHub thì ghép vào: thêm `kiemtra/kpi-tho.js` (lớp 4 — số học KPI đáp ứng thợ, 2039 phép thử) và móc nó vào `kiem-tra.ps1`, thêm `RA_SOAT_KPI_DAP_UNG_THO.md`, chèn hai mục 03/09 vào `NOTES.md`, và trả lại 4 khối tài liệu KPI trong `bao-tri-v2/CLAUDE.md` (`Su_Co` 33 cột, cách cộng dồn đoạn bận, trang `Tom_Tat`, hai bẫy mới ở mục 13, mục 14 lên 5 lớp).
- **Lý do:** Mã đi vòng qua Apps Script Editor nên về được GitHub, còn bộ kiểm tra tại máy và tài liệu giải thích thì không đi đường đó. Nếu `git pull` đè lên là mất hẳn bộ 2039 phép thử KPI.
- **Trạng thái:** Đã ghép và **đã push GitHub**. `kiemtra\kiem-tra.ps1` SẠCH cả 4 lớp, lớp KPI 2039/2039. **Không** `clasp push`, **không** deploy; hệ thống đang chạy ở nhà máy không đổi gì. Giữ nguyên mục "Đã làm xong, chưa deploy" và khối ghi chú clasp mà máy công ty viết sáng nay; chỉ bỏ dòng "So sánh với tháng trước" trong danh sách chờ vì việc đó đã xong.
- **Việc cần làm tiếp theo:** Cải tiến cách tính tỉ lệ hiệu dụng A trong `HieuDung.gs` rồi mới bật lại `HIEN_HIEU_DUNG_BAO_CAO_NGAY`. Trước đó vẫn còn nợ việc chạy trong Sheet: 🧪 Chạy test logic → 🎯 Tính lại KPI đáp ứng của thợ → 📤 Xuất báo cáo tháng 8 để xem `Tom_Tat` có vừa một trang in không.

## [2026-09-09] Kéo mã từ Apps Script Editor về repository và dọn tài liệu cũ
- **Thay đổi:** `clasp clone` ra thư mục tạm rồi chép 21 file của Bảo trì v2 về repository: 6 file khác nhau, +1006 dòng / −45 dòng. Về thêm khối KPI thợ (`tinhLaiKpiTho`, `kpiThoChoPhieu_`, `nguongKpi_`, `phutBanTrongCho_`, menu `menuTinhLaiKpi`) và khối báo cáo so sánh kỳ trước (`soSanhKy_`, `ghiTomTat_`, `gomKpiTho_`, `tyLeDatKpi_`, `tbDapUngSuCo_`, `phanVi_`). Dọn tài liệu: sửa cây thư mục trong `CLAUDE.md` cho khớp repository hiện tại, bỏ mục "KPI đáp ứng chưa bắt đầu" vì đã làm xong, chuyển "so sánh tháng trước" khỏi danh sách đang chờ trong `bao-tri-v2/CLAUDE.md`, ghi thêm tài khoản clasp mới và bẫy PowerShell chặn `clasp.ps1`.
- **Lý do:** Repository tụt lại so với Apps Script Editor vì có người sửa thẳng trên Editor. Đẩy repository lên lúc đó sẽ xoá mất khối KPI trên hệ thống đang chạy. Tài liệu cũng ghi sai trạng thái nên khó theo dõi việc còn lại.
- **Trạng thái:** Đã kéo về và commit. Bộ kiểm tra tĩnh báo sạch. **Không** `clasp push`, **không** deploy; hệ thống đang chạy trên máy nhà máy không thay đổi gì. Đã kiểm hai chiều: không có hàm nào của repository bị mất ở bản kéo về. Tỉ lệ hiệu dụng vẫn tắt (`HIEN_HIEU_DUNG_BAO_CAO_NGAY = false`).
- **Việc cần làm tiếp theo:** Bắt đầu cải tiến cách tính tỉ lệ hiệu dụng rồi mới bật lại công tắc. Mỗi phiên làm việc kéo mã về so trước khi sửa.

## [2026-09-03] Thêm sheet Tom_Tat — trang một mặt cho sếp
- **Thay đổi:** `XuatBaoCao.gs` thêm `ghiTomTat_` tạo sheet `Tom_Tat` đứng đầu file xuất (nay 8 sheet): 4 ô số lớn kèm mức tăng/giảm so với kỳ trước (xanh/đỏ/xám), khối "Điều cần biết" tự sinh, 5 máy nằm im lâu nhất, bảng thợ 4 cột với dòng dưới 70% tô đỏ. Thêm `dichNgay_`, `soSanhKy_`, `tbDapUngSuCo_`, `tyLeDatKpi_`. `xuatBaoCao` đọc `Su_Co` một lần rồi lọc hai lần để dựng kỳ liền trước dài đúng bằng kỳ báo cáo và cùng bộ lọc bộ phận/thợ. Viết lại `dichNgay_` và `dsNgayTrongKy_` bằng `Date.UTC` thay vì `Utilities.formatDate`. `Test.gs` thêm mục 8c, `kiemtra/kpi-tho.js` thêm ca 10.
- **Lý do:** Chủ dự án phản hồi báo cáo gửi sếp quá nhiều thông tin, sếp chỉ cần kết quả. `Bao_Cao` vốn được làm cho sếp nhưng đã phình thành nhật ký từng ngày + khối chọn ngưỡng + bảng thợ 12 cột, nên tách hẳn hai vai: `Tom_Tat` cho người duyệt, `Bao_Cao` cho tổ bảo trì tra cứu. Con số không có mốc so sánh thì không dùng để ra quyết định được, nên mỗi ô số lớn đều kèm mức thay đổi.
- **Trạng thái:** Đã xong tại máy local, `kiemtra\kiem-tra.ps1` SẠCH cả 4 lớp, lớp KPI 2039/2039. Viết lại `dsNgayTrongKy_` là thay đổi có rủi ro lan rộng nhất trong đợt này — nó được dùng ở cả chặn 400 ngày, nhật ký từng ngày và tỉ lệ hiệu dụng; đã canh bằng test nhưng **chưa chạy trong Sheet**. Đã xuất thật tháng 8/2026: bố cục, cột và bảng máy/thợ lên đúng. **Nhưng hai cột KPI trong bảng thợ toàn dấu gạch ngang** vì chưa chạy "🎯 Tính lại KPI đáp ứng của thợ" — cột `Phut_KPI_Tho` trên `Su_Co` còn trống. Không phải lỗi đọc dữ liệu (`docToanBoSuCo_` và `docSuCoVaLuuTru_` đều đọc đủ `HEADER_SU_CO.length` = 33 cột), mà là lỗi thiết kế của trang: `Bao_Cao` có dòng cảnh báo "chưa tính KPI" còn `Tom_Tat` thì không, nên trang cho sếp hiện cột trống mà không nói vì sao. Đã thêm dòng cảnh báo đỏ đầu khối "Điều cần biết" của `Tom_Tat`, push lúc 15:02:22.
- **Việc cần làm tiếp theo:** Chạy 🧪 Chạy test logic, rồi 📤 Xuất báo cáo tháng 8 và xem `Tom_Tat` có vừa một trang in không. Nếu tên máy bị cắt thì nới cột A; nếu khối "Điều cần biết" dài quá thì cắt bớt dòng.

## [2026-09-03] KPI đáp ứng thợ: cộng dồn đoạn bận + khối chọn ngưỡng
- **Thay đổi:** Thêm 5 cột cuối sheet `Su_Co` (`Phut_Ban_Thuc_Te`, `Phut_KPI_Tho`, `So_Doan_Ban`, `KPI_Ap_Dung`, `Dat_Nguong`) và khoá `Cau_Hinh.NGUONG_KPI_DAP_UNG_PHUT` (để trống = chưa chốt). `LuongTho.gs` thêm `phutBanTrongCho_`, `kpiThoChoPhieu_`, `nguongKpi_`, `tinhLaiKpiTho()` (menu 🎯) và ghi 5 cột này trong `acceptIncident`. `XuatBaoCao.gs` thêm khối "CƠ SỞ ĐỂ CHỌN NGƯỠNG" (trung vị/P75/P90/P95 + tỷ lệ đạt ở 5 mức 5/10/15/20/30 phút), 2 cột "KPI thợ TB" và "Tỷ lệ đạt" trong bảng theo thợ, 2 cột KPI ở `Data_Goc`. `Test.gs` thêm mục 8b. Thêm `kiemtra/kpi-tho.js` và nối thành lớp 4 của `kiem-tra.ps1` — chạy số học KPI ngay tại máy, không cần mở Sheet. Ba cột KPI cũ giữ nguyên, không sửa đè.
- **Lý do:** Công ty yêu cầu KPI không tính thời gian thợ đang bận. Cột `Phut_Dap_Ung_Thuc` cũ miễn trừ bằng MỘT mốc rảnh nên rộng tay ở hai chỗ: (1) thợ bận nhiều đoạn rời thì mọi khoảng rảnh xen giữa cũng được miễn; (2) thợ nhận việc cũ SAU lúc máy này báo hỏng thì khoảng thợ còn rảnh cũng được miễn. Cả hai đều nghiêng về phía có lợi cho người bị đo.
- **Trạng thái:** Đã xong tại máy local. `kiemtra\kiem-tra.ps1` báo SẠCH cả 4 lớp, lớp KPI đạt 2027/2027 (gồm 2000 ca ngẫu nhiên chốt đẳng thức `đáp ứng = bận + KPI` và không có số phút âm). **Chưa chạy được ~230 test trong Sheet** (cần mở Google Sheet) và **chưa đo trên dữ liệu thật** — số liệu tháng 8 chỉ có sau khi chạy backfill trên Sheet. Đã `clasp push --force` lúc 14:21:52 ngày 03/09/2026. Lần chạy test đầu tiên trong Sheet báo **3/265 hỏng**, đều là lỗi kỳ vọng trong `Test.gs` chứ không phải lỗi công thức: hai ca KPI còn kỳ vọng chuỗi cũ (`KHONG — CONG_VIEC` thay vì `KHONG — phiếu CONG_VIEC`) vì tôi sửa ở `kiemtra/kpi-tho.js` mà quên sửa bản trong `Test.gs`; ca thứ ba canh `HEADER_DATA_GOC` đúng 30 cột trong khi đã thêm 2 cột KPI. Đã sửa cả ba, thêm test canh tên 2 cột mới, ghi 2 bẫy này vào `CLAUDE.md` mục 13, và push lại lúc 14:31:20. **Chưa deploy**: trang `/exec`, QR và link cá nhân của thợ vẫn dùng deployment cũ, nên đường ghi KPI tự động lúc thợ bấm nhận chưa có hiệu lực với người dùng thật. Menu trong Sheet thì dùng ngay được vì chạy bằng code HEAD.
- **Việc cần làm tiếp theo:** `clasp push --force`, rồi trong Sheet chạy "1. Cài đặt hệ thống" (nới `Su_Co` lên 33 cột) → "🧪 Chạy test logic" → "🎯 Tính lại KPI đáp ứng của thợ" → "📤 Xuất báo cáo" tháng 8. Bốn bước này KHÔNG cần deploy vì menu chạy bằng code HEAD. Phần ghi tự động lúc thợ bấm nhận nằm trong `acceptIncident` nên chỉ có hiệu lực sau khi deploy — ghép vào lần deploy chủ nhật cùng ô tick "Máy chưa chạy lại được"; trong lúc chờ, chạy lại backfill là bù đủ. Công ty xem báo cáo tháng rồi chốt ngưỡng, điền vào `Cau_Hinh` và xuất lại.


## [2026-08-28] Deploy ẩn tỉ lệ hiệu dụng bị chặn quyền Workspace
- **Thay đổi:** Thử cập nhật đúng deployment hiện có `@2` bằng `clasp deploy --deploymentId`; không tạo deployment mới và không dùng `@HEAD`.
- **Lý do:** Cần đưa thay đổi ẩn tỉ lệ hiệu dụng từ Apps Script Editor lên trang web `/exec` mà vẫn giữ nguyên URL/QR.
- **Trạng thái:** Bị chặn trước khi triển khai với thông báo chỉ người dùng cùng domain với chủ sở hữu script mới được deploy. Deployment vẫn ở `@2`, ID và URL không thay đổi; mã mới đã có trên Apps Script Editor nhưng ứng dụng web đang chạy thật vẫn dùng version cũ.
- **Việc cần làm tiếp theo:** Đăng nhập clasp bằng tài khoản thuộc đúng Workspace domain của chủ sở hữu, hoặc nhờ chủ sở hữu mở Manage deployments và cập nhật chính deployment hiện có sang version mới; không chọn New deployment.

## [2026-08-28] Đã đẩy mã ẩn tỉ lệ hiệu dụng lên Apps Script Editor
- **Thay đổi:** Chạy `clasp push --force` trong `bao-tri-v2/`; 21 file của project Bảo trì v2 đã được cập nhật trên Apps Script Editor, bao gồm công tắc ẩn tỉ lệ hiệu dụng trong báo cáo hằng ngày.
- **Lý do:** Chủ dự án yêu cầu đưa thay đổi đã lưu trên GitHub lên Apps Script Editor.
- **Trạng thái:** Đã xong `clasp push` lúc 15:19:36 ngày 28/08/2026 sau khi bộ kiểm tra tĩnh báo sạch. Chưa deploy web app; trang `/exec`, ứng dụng đang chạy thật và QR vẫn dùng version deployment cũ.
- **Việc cần làm tiếp theo:** Khi được chủ dự án yêu cầu, deploy bằng đúng deployment ID hiện có rồi kiểm tra báo cáo hằng ngày; tuyệt đối không tạo deployment mới.

## [2026-08-28] Tách mã nguồn Bảo trì khỏi repository QC
- **Thay đổi:** Tạo repository private `knstepforward-sys/saphia-bao-tri-v2`; chuyển `bao-tri-v2/`, `apps-script/`, `kiemtra/` và tài liệu bảo trì sang repository mới bằng lịch sử Git đã lọc. Bổ sung `README.md`, `AGENTS.md`; cập nhật bộ kiểm tra để wrapper `baocao-saphia` là thành phần ngoài repository và chỉ kiểm tra khi thư mục đó có mặt trên máy.
- **Lý do:** Mã QC và mã Bảo trì trước đây dùng chung repository `saphia-qc`, làm lịch sử và trạng thái triển khai dễ bị nhầm lẫn.
- **Trạng thái:** Đã xong. Repository Bảo trì đã push và xác minh private; repository `saphia-qc` đã bỏ mã Bảo trì bằng commit riêng và chỉ còn phạm vi QC. Chưa `clasp push` và chưa deploy nên ứng dụng đang chạy thật chưa thay đổi.
- **Việc cần làm tiếp theo:** Mọi thay đổi Bảo trì thực hiện trong repository này; mọi thay đổi QC thực hiện tại `knstepforward-sys/saphia-qc`.

## [2026-08-28] Tạm tắt tỉ lệ hiệu dụng trong báo cáo hằng ngày
- **Thay đổi:** Đặt công tắc `HIEN_HIEU_DUNG_BAO_CAO_NGAY = false` trong `bao-tri-v2/BaoCaoNgay.gs` để không chạy phép tính A; `bao-tri-v2/TrangNgay.html` ẩn toàn bộ khối tỉ lệ hiệu dụng; `bao-tri-v2/CLAUDE.md` được cập nhật trạng thái và cách bật lại.
- **Lý do:** Cách tính hiện tại đang coi mọi máy có `Hoat_Dong = TRUE` là có kế hoạch chạy đủ lịch bộ phận, trong khi thực tế chỉ một số máy được bố trí chạy theo ngày/ca. Cần thiết kế quy trình tổ trưởng khai máy có kế hoạch chạy trước khi sử dụng chỉ số này.
- **Trạng thái:** Đã xong, đã qua bộ kiểm tra cú pháp/tĩnh tại máy local và đã được chủ dự án xác nhận lưu lên GitHub private. Chưa `clasp push` lên Apps Script Editor và chưa deploy; bản đang chạy thật chưa thay đổi.
- **Việc cần làm tiếp theo:** Nếu muốn áp dụng lên hệ thống thật, thực hiện `clasp push` và deploy phiên bản mới. Lập kế hoạch riêng cho chức năng khai máy chạy và người vắng theo ngày/ca trước khi bật lại chỉ số A.
